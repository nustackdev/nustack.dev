---
title: storage
sidebarTitle: 📄 storage
---

import { Cards, Callout, Tabs } from 'nextra/components'

# 📄 `loomistd.tree_storage._core.storage` Reference

## Classes

<Cards>
  <Cards.Card title="TreeStorage" href="#treestorage"/>
</Cards>

---

<a id='treestorage'></a>

## TreeStorage 🧩

<Callout type='info'>
  Inherits from: `DictOperations`, `ListOperations`
</Callout>

Hierarchical tree storage system built on top of a flat key-value store.

This class provides a complete tree-based storage interface that maps complex
nested Python objects (dictionaries and lists) to a flat key-value storage system,
preserving their structure and relationships while enabling efficient access
to specific nodes within the tree.

TreeStorage combines the functionality from both DictOperations and ListOperations
to provide a complete API for working with hierarchical data structures. Dictionary
objects become nodes with named branches, while list objects become ordered
sequences of elements.

The tree structure allows efficient traversal and manipulation of specific paths
within the tree without having to retrieve the entire structure.

```python filename='class.py'
class TreeStorage(DictOperations, ListOperations):
    ...
```

### Methods

<Tabs items={['copy_node', 'delete_node', 'dict', 'dict_contains', 'dict_delete', 'dict_get', 'dict_items', 'dict_keys', 'dict_set', 'dict_to_dict', 'dict_values', 'exists', 'filter_node', 'is_dict', 'is_list', 'list', 'list_append', 'list_extend', 'list_get', 'list_insert', 'list_length', 'list_remove', 'list_set', 'list_to_list', 'move_node', 'store_python_object', 'to_python_object', 'transform_node']}>
<Tabs.Tab>

### copy_node() 🔧

```python filename='method.py'
def copy_node(self, source_path: 'TreePath', target_path: 'TreePath', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'None'
```

Copy a node from one path to another.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `source_path` | `TreePath` | Path of the source node | `Required` |
| `target_path` | `TreePath` | Path where to copy the node | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `StorageKeyError` | If the source path doesn't exist |

</Tabs.Tab>

<Tabs.Tab>

### delete_node() 🔧

```python filename='method.py'
def delete_node(self, path: 'TreePath', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'None'
```

Delete a node and all its contained values.

This method deletes the node at the specified path,
including all nested dictionary and list nodes.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `TreePath` | Path of the node to delete | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

</Tabs.Tab>

<Tabs.Tab>

### dict() 🔧

```python filename='method.py'
def dict(self, path: 'TreePath', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'TreeDict[TreeValueT]'
```

Get a proxy object for dictionary-like access to a tree node.

This method creates a TreeDict object that provides a dictionary-like
interface to interact with the data at the specified path. If the path doesn't
exist yet, it initializes an empty dictionary node. If the path exists but is not
a dictionary node, it raises an error.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `TreePath` | Base path for the dictionary node location | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction context to use | `None` |

**Returns:** `TreeDict[TreeValueT]` - TreeDict instance for accessing the dictionary node

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectTypeError` | If the path exists but is not a dictionary node |

</Tabs.Tab>

<Tabs.Tab>

### dict_contains() 🔧

```python filename='method.py'
def dict_contains(self, base_path: 'TreePath', dict_path: 'TreePath', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'bool'
```

Check if a dictionary node contains a value at the specified path.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `base_path` | `TreePath` | Base path of the dictionary node | `Required` |
| `dict_path` | `TreePath` | Path to check | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Returns:** `bool` - True if path exists, False otherwise

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectTypeError` | If base_path is not a dictionary node |

</Tabs.Tab>

<Tabs.Tab>

### dict_delete() 🔧

```python filename='method.py'
def dict_delete(self, base_path: 'TreePath', dict_path: 'TreePath', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'None'
```

Delete a value from a dictionary node at the specified path.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `base_path` | `TreePath` | Base path of the dictionary node | `Required` |
| `dict_path` | `TreePath` | Path to the value to delete | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectTypeError` | If base_path is not a dictionary node |
| `ObjectKeyError` | If dict_path doesn't exist |

</Tabs.Tab>

<Tabs.Tab>

### dict_get() 🔧

```python filename='method.py'
def dict_get(self, base_path: 'TreePath', dict_path: 'TreePath', default: 'TreeValueContainer[TreeValueT] | EmptyProtocol' = <Empty>, txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'TreeValueContainer[TreeValueT] | EmptyProtocol'
```

Get a value from a dictionary node at the specified path.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `base_path` | `TreePath` | Base path of the dictionary node | `Required` |
| `dict_path` | `TreePath` | Path to the value within the dictionary | `Required` |
| `default` | `TreeValueContainer[TreeValueT] | EmptyProtocol` | Value to return if path doesn't exist (default is Empty) | `<Empty>` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Returns:** `TreeValueContainer[TreeValueT] | EmptyProtocol` - Value at the specified path or default if not found

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectTypeError` | If base_path is not a dictionary node |

</Tabs.Tab>

<Tabs.Tab>

### dict_items() 🔧

```python filename='method.py'
def dict_items(self, base_path: 'TreePath', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'list[tuple[TreePathComponent, TreeValueT]]'
```

Get all top-level key-value pairs in a dictionary node.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `base_path` | `TreePath` | Base path of the dictionary node | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Returns:** `list[tuple[TreePathComponent, TreeValueT]]` - List of (key, value) tuples

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectTypeError` | If base_path is not a dictionary node |

</Tabs.Tab>

<Tabs.Tab>

### dict_keys() 🔧

```python filename='method.py'
def dict_keys(self, base_path: 'TreePath', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'list[TreePathComponent]'
```

Get all top-level keys in a dictionary node.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `base_path` | `TreePath` | Base path of the dictionary node | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Returns:** `list[TreePathComponent]` - List of top-level keys

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectTypeError` | If base_path is not a dictionary node |

</Tabs.Tab>

<Tabs.Tab>

### dict_set() 🔧

```python filename='method.py'
def dict_set(self, base_path: 'TreePath', dict_path: 'TreePath', value: 'TreeValueT', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'None'
```

Set a value in a dictionary node at the specified path.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `base_path` | `TreePath` | Base path of the dictionary node | `Required` |
| `dict_path` | `TreePath` | Path where to set the value | `Required` |
| `value` | `TreeValueT` | Value to set | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectTypeError` | If base_path is not a dictionary node |

</Tabs.Tab>

<Tabs.Tab>

### dict_to_dict() 🔧

```python filename='method.py'
def dict_to_dict(self, base_path: 'TreePath', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'dict[TreePathComponent, TreeValueT]'
```

Convert a stored dictionary node to a regular Python dictionary.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `base_path` | `TreePath` | Base path of the dictionary node | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Returns:** `dict[TreePathComponent, TreeValueT]` - Dict containing all data from the stored dictionary node

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectTypeError` | If base_path is not a dictionary node |

</Tabs.Tab>

<Tabs.Tab>

### dict_values() 🔧

```python filename='method.py'
def dict_values(self, base_path: 'TreePath', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'list[TreeValueT]'
```

Get all top-level values in a dictionary node.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `base_path` | `TreePath` | Base path of the dictionary node | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Returns:** `list[TreeValueT]` - List of top-level values

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectTypeError` | If base_path is not a dictionary node |

</Tabs.Tab>

<Tabs.Tab>

### exists() 🔧

```python filename='method.py'
def exists(self, path: 'TreePath', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'bool'
```

Check if a path exists in the tree.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `TreePath` | Path to check | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Returns:** `bool` - True if the path exists, False otherwise

</Tabs.Tab>

<Tabs.Tab>

### filter_node() 🔧

```python filename='method.py'
def filter_node(self, path: 'TreePath', filter_func: 'Callable[[TreeValueT], bool] | Callable[[str, TreeValueT], bool]', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'None'
```

Filter elements of a list or dictionary node using a filter function.

For list nodes, the filter_func takes (value) and returns a boolean.
For dictionary nodes, the filter_func takes (key, value) and returns a boolean.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `TreePath` | Path of the node to filter | `Required` |
| `filter_func` | `Callable[[TreeValueT], bool] | Callable[[str, TreeValueT], bool]` | Function that takes elements and returns whether to keep them | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectTypeError` | If the node is neither a list nor a dictionary |
| `StorageKeyError` | If the path doesn't exist |

</Tabs.Tab>

<Tabs.Tab>

### is_dict() 🔧

```python filename='method.py'
def is_dict(self, path: 'TreePath', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'bool'
```

Check if a path contains a dictionary node.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `TreePath` | Path to check | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Returns:** `bool` - True if the path contains a dictionary node, False otherwise

</Tabs.Tab>

<Tabs.Tab>

### is_list() 🔧

```python filename='method.py'
def is_list(self, path: 'TreePath', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'bool'
```

Check if a path contains a list node.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `TreePath` | Path to check | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Returns:** `bool` - True if the path contains a list node, False otherwise

</Tabs.Tab>

<Tabs.Tab>

### list() 🔧

```python filename='method.py'
def list(self, path: 'TreePath', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'TreeList[TreeValueT]'
```

Get a proxy object for list-like access to a tree node.

This method creates a TreeList object that provides a list-like
interface to interact with the data at the specified path. If the path doesn't
exist yet, it initializes an empty list node. If the path exists but is not
a list node, it raises an error.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `TreePath` | Base path for the list node location | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction context to use | `None` |

**Returns:** `TreeList[TreeValueT]` - TreeList instance for accessing the list node

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectTypeError` | If the path exists but is not a list node |

</Tabs.Tab>

<Tabs.Tab>

### list_append() 🔧

```python filename='method.py'
def list_append(self, base_path: 'TreePath', value: 'TreeValueT', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'int'
```

Append an item to a list node.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `base_path` | `TreePath` | Base path of the list node | `Required` |
| `value` | `TreeValueT` | Value to append | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Returns:** `int` - New length of the list

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectTypeError` | If base_path is not a list node |

</Tabs.Tab>

<Tabs.Tab>

### list_extend() 🔧

```python filename='method.py'
def list_extend(self, base_path: 'TreePath', values: 'list[TreeValueT]', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'int'
```

Extend a list node with multiple values.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `base_path` | `TreePath` | Base path of the list node | `Required` |
| `values` | `list[TreeValueT]` | Values to append | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Returns:** `int` - New length of the list

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectTypeError` | If base_path is not a list node |

</Tabs.Tab>

<Tabs.Tab>

### list_get() 🔧

```python filename='method.py'
def list_get(self, base_path: 'TreePath', index: 'int', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'TreeValueContainer[TreeValueT]'
```

Get an item from a list node at the specified index.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `base_path` | `TreePath` | Base path of the list node | `Required` |
| `index` | `int` | Index of the item to retrieve | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Returns:** `TreeValueContainer[TreeValueT]` - Value at the specified index

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectTypeError` | If base_path is not a list node |
| `ObjectIndexError` | If index is out of range |

</Tabs.Tab>

<Tabs.Tab>

### list_insert() 🔧

```python filename='method.py'
def list_insert(self, base_path: 'TreePath', index: 'int', value: 'TreeValueT', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'None'
```

Insert an item at a specific position in a list node.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `base_path` | `TreePath` | Base path of the list node | `Required` |
| `index` | `int` | Position to insert the value | `Required` |
| `value` | `TreeValueT` | Value to insert | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectTypeError` | If base_path is not a list node |
| `ObjectIndexError` | If index is out of range |

</Tabs.Tab>

<Tabs.Tab>

### list_length() 🔧

```python filename='method.py'
def list_length(self, base_path: 'TreePath', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'int'
```

Get the length of a list node.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `base_path` | `TreePath` | Base path of the list node | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Returns:** `int` - Number of items in the list

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectTypeError` | If base_path is not a list node |

</Tabs.Tab>

<Tabs.Tab>

### list_remove() 🔧

```python filename='method.py'
def list_remove(self, base_path: 'TreePath', index: 'int', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'None'
```

Remove an item from a list node at the specified index.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `base_path` | `TreePath` | Base path of the list node | `Required` |
| `index` | `int` | Index of the item to remove | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectTypeError` | If base_path is not a list node |
| `ObjectIndexError` | If index is out of range |

</Tabs.Tab>

<Tabs.Tab>

### list_set() 🔧

```python filename='method.py'
def list_set(self, base_path: 'TreePath', index: 'int', value: 'TreeValueT', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'None'
```

Set an item in a list node at the specified index.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `base_path` | `TreePath` | Base path of the list node | `Required` |
| `index` | `int` | Index of the item to set | `Required` |
| `value` | `TreeValueT` | Value to set | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectTypeError` | If base_path is not a list node |
| `ObjectIndexError` | If index is out of range |

</Tabs.Tab>

<Tabs.Tab>

### list_to_list() 🔧

```python filename='method.py'
def list_to_list(self, base_path: 'TreePath', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'list[TreeValueT]'
```

Convert a stored list node to a regular Python list.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `base_path` | `TreePath` | Base path of the list node | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Returns:** `list[TreeValueT]` - List containing all items from the stored list node

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectTypeError` | If base_path is not a list node |

</Tabs.Tab>

<Tabs.Tab>

### move_node() 🔧

```python filename='method.py'
def move_node(self, source_path: 'TreePath', target_path: 'TreePath', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'None'
```

Move a node from one path to another.

This operation is atomic - either the move completes successfully,
or no changes are made to the tree.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `source_path` | `TreePath` | Path of the source node | `Required` |
| `target_path` | `TreePath` | Path where to move the node | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `StorageKeyError` | If the source path doesn't exist |

</Tabs.Tab>

<Tabs.Tab>

### store_python_object() 🔧

```python filename='method.py'
def store_python_object(self, path: 'TreePath', value: 'TreeValueT', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'None'
```

Store a Python object at the specified path in the tree.

This method detects the type of the provided value and stores it
appropriately as a dictionary node, list node, or primitive value.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `TreePath` | Path where to store the object | `Required` |
| `value` | `TreeValueT` | Python object to store (dict, list, or primitive value) | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `TypeError` | If the value is of an unsupported type |

</Tabs.Tab>

<Tabs.Tab>

### to_python_object() 🔧

```python filename='method.py'
def to_python_object(self, path: 'TreePath', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'Any'
```

Convert a stored tree node to a standard Python object.

This method detects the type of the node at the specified path and converts
it to the appropriate Python object (dict, list, or primitive value).

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `TreePath` | Path of the node to convert | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Returns:** `Any` - The Python representation of the stored node

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `StorageKeyError` | If the path doesn't exist |

</Tabs.Tab>

<Tabs.Tab>

### transform_node() 🔧

```python filename='method.py'
def transform_node(self, path: 'TreePath', transform_func: 'Callable[[TreeValueT], TreeValueT]', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'None'
```

Apply a transformation function to a tree node.

The transformation function takes a Python object and returns
a new transformed Python object.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `TreePath` | Path of the node to transform | `Required` |
| `transform_func` | `Callable[[TreeValueT], TreeValueT]` | Function that takes a Python object and returns a transformed version | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `StorageKeyError` | If the path doesn't exist |
| `TypeError` | If the transformation result is not of a compatible type |

</Tabs.Tab>

</Tabs>

### Source Code

```python filename='source.py'
class TreeStorage(DictOperations[TreeValueT], ListOperations[TreeValueT]):
    """
    Hierarchical tree storage system built on top of a flat key-value store.

    This class provides a complete tree-based storage interface that maps complex
    nested Python objects (dictionaries and lists) to a flat key-value storage system,
    preserving their structure and relationships while enabling efficient access
    to specific nodes within the tree.

    TreeStorage combines the functionality from both DictOperations and ListOperations
    to provide a complete API for working with hierarchical data structures. Dictionary
    objects become nodes with named branches, while list objects become ordered
    sequences of elements.

    The tree structure allows efficient traversal and manipulation of specific paths
    within the tree without having to retrieve the entire structure.
    """

    def __init__(self, storage: AsyncStorageProtocol[TreeValueT]):
        """
        Initialize the tree storage wrapper.

        Args:
            storage: The underlying key-value storage implementation
        """
        super().__init__(storage)

    # --- Public tree node object methods --- #

    async def dict(
        self,
        path: TreePath,
        txn: AsyncTransactionProtocol[TreeValueT] | None = None,
    ) -> TreeDict[TreeValueT]:
        """
        Get a proxy object for dictionary-like access to a tree node.

        This method creates a TreeDict object that provides a dictionary-like
        interface to interact with the data at the specified path. If the path doesn't
        exist yet, it initializes an empty dictionary node. If the path exists but is not
        a dictionary node, it raises an error.

        Args:
            path: Base path for the dictionary node location
            txn: Optional transaction context to use

        Returns:
            TreeDict instance for accessing the dictionary node

        Raises:
            ObjectTypeError: If the path exists but is not a dictionary node
        """
        if txn is None:
            async with await self._storage.transaction() as new_txn:
                # Initialize or verify the dictionary node
                await self._ensure_dict(path, new_txn)
            return TreeDict(self, path)
        else:
            # Use the provided transaction
            await self._ensure_dict(path, txn)
            return TreeDict(self, path, txn)

    async def list(
        self,
        path: TreePath,
        txn: AsyncTransactionProtocol[TreeValueT] | None = None,
    ) -> TreeList[TreeValueT]:
        """
        Get a proxy object for list-like access to a tree node.

        This method creates a TreeList object that provides a list-like
        interface to interact with the data at the specified path. If the path doesn't
        exist yet, it initializes an empty list node. If the path exists but is not
        a list node, it raises an error.

        Args:
            path: Base path for the list node location
            txn: Optional transaction context to use

        Returns:
            TreeList instance for accessing the list node

        Raises:
            ObjectTypeError: If the path exists but is not a list node
        """
        if txn is None:
            async with await self._storage.transaction() as new_txn:
                # Initialize or verify the list node
                await self._ensure_list(path, new_txn)
            return TreeList(self, path)
        else:
            # Use the provided transaction
            await self._ensure_list(path, txn)
            return TreeList(self, path, txn)

    async def delete_node(
        self,
        path: TreePath,
        txn: AsyncTransactionProtocol[TreeValueT] | None = None,
    ) -> None:
        """
        Delete a node and all its contained values.

        This method deletes the node at the specified path,
        including all nested dictionary and list nodes.

        Args:
            path: Path of the node to delete
            txn: Optional transaction to use
        """
        if txn is None:
            async with await self._storage.transaction() as new_txn:
                await self._delete_node(path, new_txn)
        else:
            await self._delete_node(path, txn)

    # --- Utility method for recursive conversion --- #

    async def to_python_object(
        self,
        path: TreePath,
        txn: AsyncTransactionProtocol[TreeValueT] | None = None,
    ) -> Any:
        """
        Convert a stored tree node to a standard Python object.

        This method detects the type of the node at the specified path and converts
        it to the appropriate Python object (dict, list, or primitive value).

        Args:
            path: Path of the node to convert
            txn: Optional transaction to use

        Returns:
            The Python representation of the stored node

        Raises:
            StorageKeyError: If the path doesn't exist
        """
        if txn is None:
            async with await self._storage.transaction() as new_txn:
                return await self._to_python_object(path, new_txn)
        else:
            return await self._to_python_object(path, txn)

    async def _to_python_object(
        self,
        path: TreePath,
        txn: AsyncTransactionProtocol[TreeValueT],
    ) -> Any:
        """
        Internal implementation for converting a stored tree node.

        Args:
            path: Path of the node to convert
            txn: Transaction to use

        Returns:
            The Python representation of the stored node

        Raises:
            StorageKeyError: If the path doesn't exist
        """
        node_type = await self._get_node_type(path, txn)

        if node_type == self._TYPE_DICT:
            # It's a dictionary node
            return await self._dict_to_dict(path, txn)
        elif node_type == self._TYPE_LIST:
            # It's a list node
            return await self._list_to_list(path, txn)
        else:
            # It's a primitive value node
            return await self._storage_get(path, txn)

    # --- Utility method for storing Python objects --- #

    async def store_python_object(
        self,
        path: TreePath,
        value: TreeValueT,
        txn: AsyncTransactionProtocol[TreeValueT] | None = None,
    ) -> None:
        """
        Store a Python object at the specified path in the tree.

        This method detects the type of the provided value and stores it
        appropriately as a dictionary node, list node, or primitive value.

        Args:
            path: Path where to store the object
            value: Python object to store (dict, list, or primitive value)
            txn: Optional transaction to use

        Raises:
            TypeError: If the value is of an unsupported type
        """
        if txn is None:
            async with await self._storage.transaction() as new_txn:
                await self._store_python_object(path, value, new_txn)
        else:
            await self._store_python_object(path, value, txn)

    async def _store_python_object(
        self,
        path: TreePath,
        value: TreeValueT,
        txn: AsyncTransactionProtocol[TreeValueT],
    ) -> None:
        """
        Internal implementation for storing a Python object in the tree.

        Args:
            path: Path where to store the object
            value: Python object to store
            txn: Transaction to use

        Raises:
            TypeError: If the value is of an unsupported type
        """
        # First, delete any existing data at this path
        try:
            await self._delete_node(path, txn)
        except StorageKeyError:
            pass  # Path doesn't exist, nothing to delete

        # Store the value according to its type
        if isinstance(value, dict):
            await self._set_dict(path, value, txn)
        elif isinstance(value, list):
            await self._set_list(path, value, txn)
        elif value is None or isinstance(value, (str, int, float, bool, bytes)):
            # These are the primitive types that can be stored directly
            await self._storage_set(path, cast(TreeValueT, value), txn)
        else:
            # Unsupported type
            raise TypeError(f"Cannot store object of type {type(value).__name__}")

    # --- Utility methods for type checking --- #

    async def is_dict(
        self,
        path: TreePath,
        txn: AsyncTransactionProtocol[TreeValueT] | None = None,
    ) -> bool:
        """
        Check if a path contains a dictionary node.

        Args:
            path: Path to check
            txn: Optional transaction to use

        Returns:
            True if the path contains a dictionary node, False otherwise
        """
        if txn is None:
            async with await self._storage.transaction() as new_txn:
                result = await self._is_dict(path, new_txn)
            return result
        else:
            return await self._is_dict(path, txn)

    async def is_list(
        self,
        path: TreePath,
        txn: AsyncTransactionProtocol[TreeValueT] | None = None,
    ) -> bool:
        """
        Check if a path contains a list node.

        Args:
            path: Path to check
            txn: Optional transaction to use

        Returns:
            True if the path contains a list node, False otherwise
        """
        if txn is None:
            async with await self._storage.transaction() as new_txn:
                result = await self._is_list(path, new_txn)
            return result
        else:
            return await self._is_list(path, txn)

    async def exists(
        self,
        path: TreePath,
        txn: AsyncTransactionProtocol[TreeValueT] | None = None,
    ) -> bool:
        """
        Check if a path exists in the tree.

        Args:
            path: Path to check
            txn: Optional transaction to use

        Returns:
            True if the path exists, False otherwise
        """
        if txn is None:
            async with await self._storage.transaction() as new_txn:
                result = await self._storage_exists(path, new_txn)
            return result
        else:
            return await self._storage_exists(path, txn)

    # --- Utility methods for tree operations --- #

    async def copy_node(
        self,
        source_path: TreePath,
        target_path: TreePath,
        txn: AsyncTransactionProtocol[TreeValueT] | None = None,
    ) -> None:
        """
        Copy a node from one path to another.

        Args:
            source_path: Path of the source node
            target_path: Path where to copy the node
            txn: Optional transaction to use

        Raises:
            StorageKeyError: If the source path doesn't exist
        """
        if txn is None:
            async with await self._storage.transaction() as new_txn:
                await self._copy_node(source_path, target_path, new_txn)
        else:
            await self._copy_node(source_path, target_path, txn)

    async def _copy_node(
        self,
        source_path: TreePath,
        target_path: TreePath,
        txn: AsyncTransactionProtocol[TreeValueT],
    ) -> None:
        """
        Internal implementation for copying a node.

        Args:
            source_path: Path of the source node
            target_path: Path where to copy the node
            txn: Transaction to use

        Raises:
            StorageKeyError: If the source path doesn't exist
        """
        # Get the node type
        try:
            node_type = await self._get_node_type(source_path, txn)

            # Delete any existing data at the target
            await self._delete_node(target_path, txn)

            if node_type == self._TYPE_DICT:
                # It's a dictionary node
                source_dict = await self._dict_to_dict(source_path, txn)
                await self._set_dict(target_path, source_dict, txn)
            elif node_type == self._TYPE_LIST:
                # It's a list node
                source_list = await self._list_to_list(source_path, txn)
                await self._set_list(target_path, source_list, txn)
            else:
                # It's a primitive value
                value = await self._storage_get(source_path, txn)
                await self._storage_set(target_path, value, txn)
        except StorageKeyError:
            # Source path doesn't exist
            raise

    async def move_node(
        self,
        source_path: TreePath,
        target_path: TreePath,
        txn: AsyncTransactionProtocol[TreeValueT] | None = None,
    ) -> None:
        """
        Move a node from one path to another.

        This operation is atomic - either the move completes successfully,
        or no changes are made to the tree.

        Args:
            source_path: Path of the source node
            target_path: Path where to move the node
            txn: Optional transaction to use

        Raises:
            StorageKeyError: If the source path doesn't exist
        """
        if txn is None:
            async with await self._storage.transaction() as new_txn:
                await self._move_node(source_path, target_path, new_txn)
        else:
            await self._move_node(source_path, target_path, txn)

    async def _move_node(
        self,
        source_path: TreePath,
        target_path: TreePath,
        txn: AsyncTransactionProtocol[TreeValueT],
    ) -> None:
        """
        Internal implementation for moving a node.

        Args:
            source_path: Path of the source node
            target_path: Path where to move the node
            txn: Transaction to use

        Raises:
            StorageKeyError: If the source path doesn't exist
        """
        # Copy the node
        await self._copy_node(source_path, target_path, txn)

        # Delete the source
        await self._delete_node(source_path, txn)

    async def transform_node(
        self,
        path: TreePath,
        transform_func: Callable[[TreeValueT], TreeValueT],
        txn: AsyncTransactionProtocol[TreeValueT] | None = None,
    ) -> None:
        """
        Apply a transformation function to a tree node.

        The transformation function takes a Python object and returns
        a new transformed Python object.

        Args:
            path: Path of the node to transform
            transform_func: Function that takes a Python object and returns a transformed version
            txn: Optional transaction to use

        Raises:
            StorageKeyError: If the path doesn't exist
            TypeError: If the transformation result is not of a compatible type
        """
        if txn is None:
            async with await self._storage.transaction() as new_txn:
                await self._transform_node(path, transform_func, new_txn)
        else:
            await self._transform_node(path, transform_func, txn)

    async def _transform_node(
        self,
        path: TreePath,
        transform_func: Callable[[TreeValueT], TreeValueT],
        txn: AsyncTransactionProtocol[TreeValueT],
    ) -> None:
        """
        Internal implementation for transforming a tree node.

        Args:
            path: Path of the node to transform
            transform_func: Function that takes a Python object and returns a transformed version
            txn: Transaction to use

        Raises:
            StorageKeyError: If the path doesn't exist
            TypeError: If the transformation result is not of a compatible type
        """
        # Get the current node as a Python object
        obj = await self._to_python_object(path, txn)

        # Apply the transformation
        transformed_obj = transform_func(obj)

        # Store the transformed object
        await self._store_python_object(path, transformed_obj, txn)

    async def filter_node(
        self,
        path: TreePath,
        filter_func: Callable[[TreeValueT], bool] | Callable[[str, TreeValueT], bool],
        txn: AsyncTransactionProtocol[TreeValueT] | None = None,
    ) -> None:
        """
        Filter elements of a list or dictionary node using a filter function.

        For list nodes, the filter_func takes (value) and returns a boolean.
        For dictionary nodes, the filter_func takes (key, value) and returns a boolean.

        Args:
            path: Path of the node to filter
            filter_func: Function that takes elements and returns whether to keep them
            txn: Optional transaction to use

        Raises:
            ObjectTypeError: If the node is neither a list nor a dictionary
            StorageKeyError: If the path doesn't exist
        """
        if txn is None:
            async with await self._storage.transaction() as new_txn:
                await self._filter_node(path, filter_func, new_txn)
        else:
            await self._filter_node(path, filter_func, txn)

    async def _filter_node(
        self,
        path: TreePath,
        filter_func: Callable[[TreeValueT], bool] | Callable[[str, TreeValueT], bool],
        txn: AsyncTransactionProtocol[TreeValueT],
    ) -> None:
        """
        Internal implementation for filtering tree nodes.

        Args:
            path: Path of the node to filter
            filter_func: Function that takes elements and returns whether to keep them
            txn: Transaction to use

        Raises:
            ObjectTypeError: If the node is neither a list nor a dictionary
            StorageKeyError: If the path doesn't exist
        """
        # Check the node type
        node_type = await self._get_node_type(path, txn)

        if node_type == self._TYPE_LIST:
            # Filter the list node
            items = await self._list_to_list(path, txn)
            filter_func_list = cast(Callable[[TreeValueT], bool], filter_func)
            filtered_items = [item for item in items if filter_func_list(item)]

            # Replace with filtered list
            await self._set_list(path, filtered_items, txn)

        elif node_type == self._TYPE_DICT:
            # Filter the dictionary node
            items = await self._dict_items(path, txn)
            filter_func_dict = cast(Callable[[str, TreeValueT], bool], filter_func)
            keys_to_delete = [k for k, v in items if not filter_func_dict(k, v)]

            # Delete filtered keys
            for k in keys_to_delete:
                await self._dict_delete(
                    path,
                    (k,),  # Single component path for the key
                    txn,
                )

        else:
            raise ObjectTypeError(
                f"Node at path {path} must be a list or dictionary node for filtering"
            )

```

## Source Code

```python filename='source.py'
from __future__ import annotations

from typing import Any, Callable, cast

from loomi.interfaces.state.kv import AsyncStorageProtocol, AsyncTransactionProtocol
from loomistd.kv_storage import StorageKeyError

from .._exceptions import ObjectTypeError
from .._nodes.tree_dict import TreeDict
from .._nodes.tree_list import TreeList
from .._types import TreePath, TreeValueT
from .dict_operations import DictOperations
from .list_operations import ListOperations

__all__ = [
    "TreeStorage",
]


class TreeStorage(DictOperations[TreeValueT], ListOperations[TreeValueT]):
    """
    Hierarchical tree storage system built on top of a flat key-value store.

    This class provides a complete tree-based storage interface that maps complex
    nested Python objects (dictionaries and lists) to a flat key-value storage system,
    preserving their structure and relationships while enabling efficient access
    to specific nodes within the tree.

    TreeStorage combines the functionality from both DictOperations and ListOperations
    to provide a complete API for working with hierarchical data structures. Dictionary
    objects become nodes with named branches, while list objects become ordered
    sequences of elements.

    The tree structure allows efficient traversal and manipulation of specific paths
    within the tree without having to retrieve the entire structure.
    """

    def __init__(self, storage: AsyncStorageProtocol[TreeValueT]):
        """
        Initialize the tree storage wrapper.

        Args:
            storage: The underlying key-value storage implementation
        """
        super().__init__(storage)

    # --- Public tree node object methods --- #

    async def dict(
        self,
        path: TreePath,
        txn: AsyncTransactionProtocol[TreeValueT] | None = None,
    ) -> TreeDict[TreeValueT]:
        """
        Get a proxy object for dictionary-like access to a tree node.

        This method creates a TreeDict object that provides a dictionary-like
        interface to interact with the data at the specified path. If the path doesn't
        exist yet, it initializes an empty dictionary node. If the path exists but is not
        a dictionary node, it raises an error.

        Args:
            path: Base path for the dictionary node location
            txn: Optional transaction context to use

        Returns:
            TreeDict instance for accessing the dictionary node

        Raises:
            ObjectTypeError: If the path exists but is not a dictionary node
        """
        if txn is None:
            async with await self._storage.transaction() as new_txn:
                # Initialize or verify the dictionary node
                await self._ensure_dict(path, new_txn)
            return TreeDict(self, path)
        else:
            # Use the provided transaction
            await self._ensure_dict(path, txn)
            return TreeDict(self, path, txn)

    async def list(
        self,
        path: TreePath,
        txn: AsyncTransactionProtocol[TreeValueT] | None = None,
    ) -> TreeList[TreeValueT]:
        """
        Get a proxy object for list-like access to a tree node.

        This method creates a TreeList object that provides a list-like
        interface to interact with the data at the specified path. If the path doesn't
        exist yet, it initializes an empty list node. If the path exists but is not
        a list node, it raises an error.

        Args:
            path: Base path for the list node location
            txn: Optional transaction context to use

        Returns:
            TreeList instance for accessing the list node

        Raises:
            ObjectTypeError: If the path exists but is not a list node
        """
        if txn is None:
            async with await self._storage.transaction() as new_txn:
                # Initialize or verify the list node
                await self._ensure_list(path, new_txn)
            return TreeList(self, path)
        else:
            # Use the provided transaction
            await self._ensure_list(path, txn)
            return TreeList(self, path, txn)

    async def delete_node(
        self,
        path: TreePath,
        txn: AsyncTransactionProtocol[TreeValueT] | None = None,
    ) -> None:
        """
        Delete a node and all its contained values.

        This method deletes the node at the specified path,
        including all nested dictionary and list nodes.

        Args:
            path: Path of the node to delete
            txn: Optional transaction to use
        """
        if txn is None:
            async with await self._storage.transaction() as new_txn:
                await self._delete_node(path, new_txn)
        else:
            await self._delete_node(path, txn)

    # --- Utility method for recursive conversion --- #

    async def to_python_object(
        self,
        path: TreePath,
        txn: AsyncTransactionProtocol[TreeValueT] | None = None,
    ) -> Any:
        """
        Convert a stored tree node to a standard Python object.

        This method detects the type of the node at the specified path and converts
        it to the appropriate Python object (dict, list, or primitive value).

        Args:
            path: Path of the node to convert
            txn: Optional transaction to use

        Returns:
            The Python representation of the stored node

        Raises:
            StorageKeyError: If the path doesn't exist
        """
        if txn is None:
            async with await self._storage.transaction() as new_txn:
                return await self._to_python_object(path, new_txn)
        else:
            return await self._to_python_object(path, txn)

    async def _to_python_object(
        self,
        path: TreePath,
        txn: AsyncTransactionProtocol[TreeValueT],
    ) -> Any:
        """
        Internal implementation for converting a stored tree node.

        Args:
            path: Path of the node to convert
            txn: Transaction to use

        Returns:
            The Python representation of the stored node

        Raises:
            StorageKeyError: If the path doesn't exist
        """
        node_type = await self._get_node_type(path, txn)

        if node_type == self._TYPE_DICT:
            # It's a dictionary node
            return await self._dict_to_dict(path, txn)
        elif node_type == self._TYPE_LIST:
            # It's a list node
            return await self._list_to_list(path, txn)
        else:
            # It's a primitive value node
            return await self._storage_get(path, txn)

    # --- Utility method for storing Python objects --- #

    async def store_python_object(
        self,
        path: TreePath,
        value: TreeValueT,
        txn: AsyncTransactionProtocol[TreeValueT] | None = None,
    ) -> None:
        """
        Store a Python object at the specified path in the tree.

        This method detects the type of the provided value and stores it
        appropriately as a dictionary node, list node, or primitive value.

        Args:
            path: Path where to store the object
            value: Python object to store (dict, list, or primitive value)
            txn: Optional transaction to use

        Raises:
            TypeError: If the value is of an unsupported type
        """
        if txn is None:
            async with await self._storage.transaction() as new_txn:
                await self._store_python_object(path, value, new_txn)
        else:
            await self._store_python_object(path, value, txn)

    async def _store_python_object(
        self,
        path: TreePath,
        value: TreeValueT,
        txn: AsyncTransactionProtocol[TreeValueT],
    ) -> None:
        """
        Internal implementation for storing a Python object in the tree.

        Args:
            path: Path where to store the object
            value: Python object to store
            txn: Transaction to use

        Raises:
            TypeError: If the value is of an unsupported type
        """
        # First, delete any existing data at this path
        try:
            await self._delete_node(path, txn)
        except StorageKeyError:
            pass  # Path doesn't exist, nothing to delete

        # Store the value according to its type
        if isinstance(value, dict):
            await self._set_dict(path, value, txn)
        elif isinstance(value, list):
            await self._set_list(path, value, txn)
        elif value is None or isinstance(value, (str, int, float, bool, bytes)):
            # These are the primitive types that can be stored directly
            await self._storage_set(path, cast(TreeValueT, value), txn)
        else:
            # Unsupported type
            raise TypeError(f"Cannot store object of type {type(value).__name__}")

    # --- Utility methods for type checking --- #

    async def is_dict(
        self,
        path: TreePath,
        txn: AsyncTransactionProtocol[TreeValueT] | None = None,
    ) -> bool:
        """
        Check if a path contains a dictionary node.

        Args:
            path: Path to check
            txn: Optional transaction to use

        Returns:
            True if the path contains a dictionary node, False otherwise
        """
        if txn is None:
            async with await self._storage.transaction() as new_txn:
                result = await self._is_dict(path, new_txn)
            return result
        else:
            return await self._is_dict(path, txn)

    async def is_list(
        self,
        path: TreePath,
        txn: AsyncTransactionProtocol[TreeValueT] | None = None,
    ) -> bool:
        """
        Check if a path contains a list node.

        Args:
            path: Path to check
            txn: Optional transaction to use

        Returns:
            True if the path contains a list node, False otherwise
        """
        if txn is None:
            async with await self._storage.transaction() as new_txn:
                result = await self._is_list(path, new_txn)
            return result
        else:
            return await self._is_list(path, txn)

    async def exists(
        self,
        path: TreePath,
        txn: AsyncTransactionProtocol[TreeValueT] | None = None,
    ) -> bool:
        """
        Check if a path exists in the tree.

        Args:
            path: Path to check
            txn: Optional transaction to use

        Returns:
            True if the path exists, False otherwise
        """
        if txn is None:
            async with await self._storage.transaction() as new_txn:
                result = await self._storage_exists(path, new_txn)
            return result
        else:
            return await self._storage_exists(path, txn)

    # --- Utility methods for tree operations --- #

    async def copy_node(
        self,
        source_path: TreePath,
        target_path: TreePath,
        txn: AsyncTransactionProtocol[TreeValueT] | None = None,
    ) -> None:
        """
        Copy a node from one path to another.

        Args:
            source_path: Path of the source node
            target_path: Path where to copy the node
            txn: Optional transaction to use

        Raises:
            StorageKeyError: If the source path doesn't exist
        """
        if txn is None:
            async with await self._storage.transaction() as new_txn:
                await self._copy_node(source_path, target_path, new_txn)
        else:
            await self._copy_node(source_path, target_path, txn)

    async def _copy_node(
        self,
        source_path: TreePath,
        target_path: TreePath,
        txn: AsyncTransactionProtocol[TreeValueT],
    ) -> None:
        """
        Internal implementation for copying a node.

        Args:
            source_path: Path of the source node
            target_path: Path where to copy the node
            txn: Transaction to use

        Raises:
            StorageKeyError: If the source path doesn't exist
        """
        # Get the node type
        try:
            node_type = await self._get_node_type(source_path, txn)

            # Delete any existing data at the target
            await self._delete_node(target_path, txn)

            if node_type == self._TYPE_DICT:
                # It's a dictionary node
                source_dict = await self._dict_to_dict(source_path, txn)
                await self._set_dict(target_path, source_dict, txn)
            elif node_type == self._TYPE_LIST:
                # It's a list node
                source_list = await self._list_to_list(source_path, txn)
                await self._set_list(target_path, source_list, txn)
            else:
                # It's a primitive value
                value = await self._storage_get(source_path, txn)
                await self._storage_set(target_path, value, txn)
        except StorageKeyError:
            # Source path doesn't exist
            raise

    async def move_node(
        self,
        source_path: TreePath,
        target_path: TreePath,
        txn: AsyncTransactionProtocol[TreeValueT] | None = None,
    ) -> None:
        """
        Move a node from one path to another.

        This operation is atomic - either the move completes successfully,
        or no changes are made to the tree.

        Args:
            source_path: Path of the source node
            target_path: Path where to move the node
            txn: Optional transaction to use

        Raises:
            StorageKeyError: If the source path doesn't exist
        """
        if txn is None:
            async with await self._storage.transaction() as new_txn:
                await self._move_node(source_path, target_path, new_txn)
        else:
            await self._move_node(source_path, target_path, txn)

    async def _move_node(
        self,
        source_path: TreePath,
        target_path: TreePath,
        txn: AsyncTransactionProtocol[TreeValueT],
    ) -> None:
        """
        Internal implementation for moving a node.

        Args:
            source_path: Path of the source node
            target_path: Path where to move the node
            txn: Transaction to use

        Raises:
            StorageKeyError: If the source path doesn't exist
        """
        # Copy the node
        await self._copy_node(source_path, target_path, txn)

        # Delete the source
        await self._delete_node(source_path, txn)

    async def transform_node(
        self,
        path: TreePath,
        transform_func: Callable[[TreeValueT], TreeValueT],
        txn: AsyncTransactionProtocol[TreeValueT] | None = None,
    ) -> None:
        """
        Apply a transformation function to a tree node.

        The transformation function takes a Python object and returns
        a new transformed Python object.

        Args:
            path: Path of the node to transform
            transform_func: Function that takes a Python object and returns a transformed version
            txn: Optional transaction to use

        Raises:
            StorageKeyError: If the path doesn't exist
            TypeError: If the transformation result is not of a compatible type
        """
        if txn is None:
            async with await self._storage.transaction() as new_txn:
                await self._transform_node(path, transform_func, new_txn)
        else:
            await self._transform_node(path, transform_func, txn)

    async def _transform_node(
        self,
        path: TreePath,
        transform_func: Callable[[TreeValueT], TreeValueT],
        txn: AsyncTransactionProtocol[TreeValueT],
    ) -> None:
        """
        Internal implementation for transforming a tree node.

        Args:
            path: Path of the node to transform
            transform_func: Function that takes a Python object and returns a transformed version
            txn: Transaction to use

        Raises:
            StorageKeyError: If the path doesn't exist
            TypeError: If the transformation result is not of a compatible type
        """
        # Get the current node as a Python object
        obj = await self._to_python_object(path, txn)

        # Apply the transformation
        transformed_obj = transform_func(obj)

        # Store the transformed object
        await self._store_python_object(path, transformed_obj, txn)

    async def filter_node(
        self,
        path: TreePath,
        filter_func: Callable[[TreeValueT], bool] | Callable[[str, TreeValueT], bool],
        txn: AsyncTransactionProtocol[TreeValueT] | None = None,
    ) -> None:
        """
        Filter elements of a list or dictionary node using a filter function.

        For list nodes, the filter_func takes (value) and returns a boolean.
        For dictionary nodes, the filter_func takes (key, value) and returns a boolean.

        Args:
            path: Path of the node to filter
            filter_func: Function that takes elements and returns whether to keep them
            txn: Optional transaction to use

        Raises:
            ObjectTypeError: If the node is neither a list nor a dictionary
            StorageKeyError: If the path doesn't exist
        """
        if txn is None:
            async with await self._storage.transaction() as new_txn:
                await self._filter_node(path, filter_func, new_txn)
        else:
            await self._filter_node(path, filter_func, txn)

    async def _filter_node(
        self,
        path: TreePath,
        filter_func: Callable[[TreeValueT], bool] | Callable[[str, TreeValueT], bool],
        txn: AsyncTransactionProtocol[TreeValueT],
    ) -> None:
        """
        Internal implementation for filtering tree nodes.

        Args:
            path: Path of the node to filter
            filter_func: Function that takes elements and returns whether to keep them
            txn: Transaction to use

        Raises:
            ObjectTypeError: If the node is neither a list nor a dictionary
            StorageKeyError: If the path doesn't exist
        """
        # Check the node type
        node_type = await self._get_node_type(path, txn)

        if node_type == self._TYPE_LIST:
            # Filter the list node
            items = await self._list_to_list(path, txn)
            filter_func_list = cast(Callable[[TreeValueT], bool], filter_func)
            filtered_items = [item for item in items if filter_func_list(item)]

            # Replace with filtered list
            await self._set_list(path, filtered_items, txn)

        elif node_type == self._TYPE_DICT:
            # Filter the dictionary node
            items = await self._dict_items(path, txn)
            filter_func_dict = cast(Callable[[str, TreeValueT], bool], filter_func)
            keys_to_delete = [k for k, v in items if not filter_func_dict(k, v)]

            # Delete filtered keys
            for k in keys_to_delete:
                await self._dict_delete(
                    path,
                    (k,),  # Single component path for the key
                    txn,
                )

        else:
            raise ObjectTypeError(
                f"Node at path {path} must be a list or dictionary node for filtering"
            )

```

