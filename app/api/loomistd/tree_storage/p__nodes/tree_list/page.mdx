---
title: tree_list
sidebarTitle: 📄 tree_list
---

import { Cards, Callout, Tabs } from 'nextra/components'

# 📄 `loomistd.tree_storage._nodes.tree_list` Reference

## Classes

<Cards>
  <Cards.Card title="TreeList" href="#treelist"/>
</Cards>

---

<a id='treelist'></a>

## TreeList 🧩

<Callout type='info'>
  Inherits from: `TreeNode`, `AsyncTreeListProtocol`
</Callout>

A list-like interface to tree storage.

This class provides an interface similar to a Python list
for interacting with list nodes in the tree storage.
It implements async methods for list operations that map
to the underlying tree structure.

```python filename='class.py'
class TreeList(TreeNode, AsyncTreeListProtocol):
    ...
```

### Properties

| Name | Description |
| ---- | ----------- |
| `is_async` | Check if the node is asynchronous. |
| `path` | Get the base path for this tree node. |

### Methods

<Tabs items={['append', 'clear', 'copy_to', 'delete', 'dict', 'exists', 'extend', 'filter', 'get', 'insert', 'is_dict', 'is_list', 'length', 'list', 'map', 'move_to', 'pop', 'remove', 'set', 'store', 'to_list', 'to_python_object', 'transform']}>
<Tabs.Tab>

### append() 🔧

```python filename='method.py'
def append(self, value: 'TreeValueT') -> 'int'
```

Append an item to the list node.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `value` | `TreeValueT` | Value to append | `Required` |

**Returns:** `int` - New length of the list

</Tabs.Tab>

<Tabs.Tab>

### clear() 🔧

```python filename='method.py'
def clear(self) -> 'None'
```

Remove all items from the list node.

</Tabs.Tab>

<Tabs.Tab>

### copy_to() 🔧

```python filename='method.py'
def copy_to(self, target: 'TreePathComponent', /, *targets: 'TreePathComponent', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'None'
```

Create a copy of this node at another location in the tree.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `target` | `TreePathComponent` | First segment of the target path *targets: Additional segments of the target path | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

</Tabs.Tab>

<Tabs.Tab>

### delete() 🔧

```python filename='method.py'
def delete(self, index: 'int') -> 'None'
```

Remove an item from the list node at the specified index.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `index` | `int` | Index of the item to remove | `Required` |

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectIndexError` | If the index is out of range |

</Tabs.Tab>

<Tabs.Tab>

### dict() 🔧

```python filename='method.py'
def dict(self, path: 'TreePathComponent', /, *paths: 'TreePathComponent', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'AsyncTreeDictProtocol[TreeValueT]'
```

Get a nested dictionary node interface.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `TreePathComponent` | First path segment *paths: Additional path segments | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Returns:** `AsyncTreeDictProtocol[TreeValueT]` - A new TreeDict instance for the nested dictionary node

</Tabs.Tab>

<Tabs.Tab>

### exists() 🔧

```python filename='method.py'
def exists(self, *args: 'Any', **kwargs: 'Any') -> 'bool'
```

Check if a node exists in the tree.

When called with no path arguments, checks if the current node exists.
When called with path segments, checks if the specified nested node exists.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `Any` | Optional first path segment *paths: Additional path segments | `Required` |
| `txn` | `Any` | Optional transaction to use | `Required` |

**Returns:** `bool` - True if the node exists, False otherwise

</Tabs.Tab>

<Tabs.Tab>

### extend() 🔧

```python filename='method.py'
def extend(self, values: 'list[TreeValueT]') -> 'int'
```

Extend the list node with multiple values.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `values` | `list[TreeValueT]` | List of values to append | `Required` |

**Returns:** `int` - New length of the list

</Tabs.Tab>

<Tabs.Tab>

### filter() 🔧

```python filename='method.py'
def filter(self, filter_func: 'Callable[[TreeValueContainer[TreeValueT]], bool] | Callable[[str, TreeValueContainer[TreeValueT]], bool]', *args: 'Any', **kwargs: 'Any') -> 'None'
```

Filter elements of a list or dictionary node.

For list nodes, filter_func takes (value) and returns a boolean.
For dictionary nodes, filter_func takes (key, value) and returns a boolean.

Elements for which filter_func returns False will be removed.

When called with just the filter function, filters the current node.
When called with path segments, filters the specified nested node.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `filter_func` | `Callable[[TreeValueContainer[TreeValueT]], bool] | Callable[[str, TreeValueContainer[TreeValueT]], bool]` | Function that takes elements and returns whether to keep them | `Required` |
| `path` | `Any` | Optional first path segment *paths: Additional path segments | `Required` |
| `txn` | `Any` | Optional transaction to use | `Required` |

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectTypeError` | If the node is neither a list nor a dictionary |
| `StorageKeyError` | If the node doesn't exist |

</Tabs.Tab>

<Tabs.Tab>

### get() 🔧

```python filename='method.py'
def get(self, index: 'int') -> 'TreeValueContainer[TreeValueT]'
```

Get an item from the list node at the specified index.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `index` | `int` | Index of the item to retrieve | `Required` |

**Returns:** `TreeValueContainer[TreeValueT]` - The item at the specified index

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectIndexError` | If the index is out of range |

</Tabs.Tab>

<Tabs.Tab>

### insert() 🔧

```python filename='method.py'
def insert(self, index: 'int', value: 'TreeValueT') -> 'None'
```

Insert an item at a specific position in the list node.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `index` | `int` | Position to insert the value | `Required` |
| `value` | `TreeValueT` | Value to insert | `Required` |

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectIndexError` | If the index is out of range |

</Tabs.Tab>

<Tabs.Tab>

### is_dict() 🔧

```python filename='method.py'
def is_dict(self, *args: 'Any', **kwargs: 'Any') -> 'bool'
```

Check if a node is a dictionary node.

When called with no path arguments, checks if the current node is a dictionary.
When called with path segments, checks if the specified nested node is a dictionary.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `Any` | Optional first path segment *paths: Additional path segments | `Required` |
| `txn` | `Any` | Optional transaction to use | `Required` |

**Returns:** `bool` - True if the node is a dictionary, False otherwise

</Tabs.Tab>

<Tabs.Tab>

### is_list() 🔧

```python filename='method.py'
def is_list(self, *args: 'Any', **kwargs: 'Any') -> 'bool'
```

Check if a node is a list node.

When called with no path arguments, checks if the current node is a list.
When called with path segments, checks if the specified nested node is a list.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `Any` | Optional first path segment *paths: Additional path segments | `Required` |
| `txn` | `Any` | Optional transaction to use | `Required` |

**Returns:** `bool` - True if the node is a list, False otherwise

</Tabs.Tab>

<Tabs.Tab>

### length() 🔧

```python filename='method.py'
def length(self) -> 'int'
```

Get the length of the list node.

**Returns:** `int` - Number of items in the list

</Tabs.Tab>

<Tabs.Tab>

### list() 🔧

```python filename='method.py'
def list(self, path: 'TreePathComponent', /, *paths: 'TreePathComponent', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'AsyncTreeListProtocol[TreeValueT]'
```

Get a nested list node interface.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `TreePathComponent` | First path segment *paths: Additional path segments | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use | `None` |

**Returns:** `AsyncTreeListProtocol[TreeValueT]` - A new TreeList instance for the nested list node

</Tabs.Tab>

<Tabs.Tab>

### map() 🔧

```python filename='method.py'
def map(self, map_func: 'Callable[[TreeValueContainer[TreeValueT]], TreeValueT]', *args: 'Any', **kwargs: 'Any') -> 'None'
```

Apply a mapping function to each element in a list or dictionary node.

For list nodes, each element is replaced with the result of map_func(element).
For dictionary nodes, each value is replaced with the result of map_func(value).

This is a convenience method that transforms the node by applying the map function
to each element while preserving the structure.

When called with just the map function, maps the current node elements.
When called with path segments, maps the specified nested node elements.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `map_func` | `Callable[[TreeValueContainer[TreeValueT]], TreeValueT]` | Function to apply to each element | `Required` |
| `path` | `Any` | Optional first path segment *paths: Additional path segments | `Required` |
| `txn` | `Any` | Optional transaction to use | `Required` |

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectTypeError` | If the node is neither a list nor a dictionary |
| `StorageKeyError` | If the node doesn't exist |
| `TypeError` | If the mapping result contains unsupported types |

</Tabs.Tab>

<Tabs.Tab>

### move_to() 🔧

```python filename='method.py'
def move_to(self, target: 'TreePathComponent', /, *targets: 'TreePathComponent', txn: 'AsyncTransactionProtocol[TreeValueT] | None' = None) -> 'None'
```

Move this node to another location in the tree.

This operation is atomic - either the move completes successfully,
or no changes are made to the tree.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `target` | `TreePathComponent` | First segment of the target path *targets: Additional segments of the target path | `Required` |
| `txn` | `AsyncTransactionProtocol[TreeValueT] | None` | Optional transaction to use Note: After this operation, the current TreeNode instance should not be used as its path is no longer valid. | `None` |

</Tabs.Tab>

<Tabs.Tab>

### pop() 🔧

```python filename='method.py'
def pop(self, index: 'int' = -1) -> 'TreeValueContainer[TreeValueT]'
```

Remove and return an item from the list node.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `index` | `int` | Index of the item to remove (default: last item) | `-1` |

**Returns:** `TreeValueContainer[TreeValueT]` - The item at the specified index

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectIndexError` | If the index is out of range |

</Tabs.Tab>

<Tabs.Tab>

### remove() 🔧

```python filename='method.py'
def remove(self, *args: 'Any', **kwargs: 'Any') -> 'None'
```

Delete a node from the tree.

When called with no path arguments, deletes the current node.
When called with path segments, deletes the specified nested node.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `Any` | Optional first path segment *paths: Additional path segments | `Required` |
| `txn` | `Any` | Optional transaction to use | `Required` |

</Tabs.Tab>

<Tabs.Tab>

### set() 🔧

```python filename='method.py'
def set(self, index: 'int', value: 'TreeValueT') -> 'None'
```

Set an item in the list node at the specified index.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `index` | `int` | Index of the item to set | `Required` |
| `value` | `TreeValueT` | Value to set | `Required` |

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObjectIndexError` | If the index is out of range |

</Tabs.Tab>

<Tabs.Tab>

### store() 🔧

```python filename='method.py'
def store(self, value: 'TreeValueT', *args: 'Any', **kwargs: 'Any') -> 'None'
```

Store a Python object at a specified node.

This is a convenience method for directly storing Python objects
(dictionaries, lists, or primitive values) in the tree.

When called with just the value, replaces the current node.
When called with path segments, stores at the specified nested node.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `value` | `TreeValueT` | Python object to store | `Required` |
| `path` | `Any` | Optional first path segment *paths: Additional path segments | `Required` |
| `txn` | `Any` | Optional transaction to use | `Required` |

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `TypeError` | If the value is of an unsupported type |

</Tabs.Tab>

<Tabs.Tab>

### to_list() 🔧

```python filename='method.py'
def to_list(self) -> 'list[TreeValueT]'
```

Convert to a regular Python list.

**Returns:** `list[TreeValueT]` - Python list containing all items from this list node

</Tabs.Tab>

<Tabs.Tab>

### to_python_object() 🔧

```python filename='method.py'
def to_python_object(self, *args: 'Any', **kwargs: 'Any') -> 'TreeValueContainer[TreeValueT]'
```

Convert a node to a standard Python object.

When called with no path arguments, converts the current node.
When called with path segments, converts the specified nested node.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `Any` | Optional first path segment *paths: Additional path segments | `Required` |
| `txn` | `Any` | Optional transaction to use | `Required` |

**Returns:** `TreeValueContainer[TreeValueT]` - The Python representation of the node

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `StorageKeyError` | If the node doesn't exist |

</Tabs.Tab>

<Tabs.Tab>

### transform() 🔧

```python filename='method.py'
def transform(self, transform_func: 'Callable[[TreeValueContainer[TreeValueT]], TreeValueT]', *args: 'Any', **kwargs: 'Any') -> 'None'
```

Apply a transformation function to a node.

The transformation function takes the current value as a Python object
and returns a new transformed Python object.

When called with just the transform function, transforms the current node.
When called with path segments, transforms the specified nested node.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `transform_func` | `Callable[[TreeValueContainer[TreeValueT]], TreeValueT]` | Function that takes a Python object and returns a transformed version | `Required` |
| `path` | `Any` | Optional first path segment *paths: Additional path segments | `Required` |
| `txn` | `Any` | Optional transaction to use | `Required` |

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `StorageKeyError` | If the node doesn't exist |
| `TypeError` | If the transformation result is not of a compatible type |

</Tabs.Tab>

</Tabs>

### Source Code

```python filename='source.py'
class TreeList(TreeNode[TreeValueT], AsyncTreeListProtocol[TreeValueT]):
    """
    A list-like interface to tree storage.

    This class provides an interface similar to a Python list
    for interacting with list nodes in the tree storage.
    It implements async methods for list operations that map
    to the underlying tree structure.
    """

    # --- Async list operations --- #

    async def get(self, index: int) -> TreeValueContainer[TreeValueT]:
        """
        Get an item from the list node at the specified index.

        Args:
            index: Index of the item to retrieve

        Returns:
            The item at the specified index

        Raises:
            ObjectIndexError: If the index is out of range
        """
        return await self._storage.list_get(self._path, index, self._txn)

    async def set(self, index: int, value: TreeValueT) -> None:
        """
        Set an item in the list node at the specified index.

        Args:
            index: Index of the item to set
            value: Value to set

        Raises:
            ObjectIndexError: If the index is out of range
        """
        await self._storage.list_set(self._path, index, value, self._txn)

    async def append(self, value: TreeValueT) -> int:
        """
        Append an item to the list node.

        Args:
            value: Value to append

        Returns:
            New length of the list
        """
        return await self._storage.list_append(self._path, value, self._txn)

    async def extend(self, values: list[TreeValueT]) -> int:
        """
        Extend the list node with multiple values.

        Args:
            values: List of values to append

        Returns:
            New length of the list
        """
        return await self._storage.list_extend(self._path, values, self._txn)

    async def insert(self, index: int, value: TreeValueT) -> None:
        """
        Insert an item at a specific position in the list node.

        Args:
            index: Position to insert the value
            value: Value to insert

        Raises:
            ObjectIndexError: If the index is out of range
        """
        await self._storage.list_insert(self._path, index, value, self._txn)

    async def delete(self, index: int) -> None:
        """
        Remove an item from the list node at the specified index.

        Args:
            index: Index of the item to remove

        Raises:
            ObjectIndexError: If the index is out of range
        """
        await self._storage.list_remove(self._path, index, self._txn)

    async def length(self) -> int:
        """
        Get the length of the list node.

        Returns:
            Number of items in the list
        """
        return await self._storage.list_length(self._path, self._txn)

    # --- List conversion and utilities --- #

    async def to_list(self) -> list[TreeValueT]:
        """
        Convert to a regular Python list.

        Returns:
            Python list containing all items from this list node
        """
        return await self._storage.list_to_list(self._path, self._txn)

    async def clear(self) -> None:
        """
        Remove all items from the list node.
        """
        # Get the current length and remove items one by one from the end
        length = await self.length()
        for i in range(length - 1, -1, -1):
            try:
                await self.delete(i)
            except ObjectIndexError:
                # Index became invalid, just skip it
                pass

    async def pop(self, index: int = -1) -> TreeValueContainer[TreeValueT]:
        """
        Remove and return an item from the list node.

        Args:
            index: Index of the item to remove (default: last item)

        Returns:
            The item at the specified index

        Raises:
            ObjectIndexError: If the index is out of range
        """
        value = await self.get(index)
        await self.delete(index)
        return value

    async def __len__(self) -> int:
        """
        Get the number of items in the list node.

        Returns:
            The number of items
        """
        return await self.length()

```

## Source Code

```python filename='source.py'
from __future__ import annotations

from typing import TYPE_CHECKING

from loomi.interfaces.state.tree import AsyncTreeListProtocol

from .._exceptions import ObjectIndexError
from .._types import TreeValueContainer, TreeValueT
from .tree_node import TreeNode

__all__ = [
    "TreeList",
]


class TreeList(TreeNode[TreeValueT], AsyncTreeListProtocol[TreeValueT]):
    """
    A list-like interface to tree storage.

    This class provides an interface similar to a Python list
    for interacting with list nodes in the tree storage.
    It implements async methods for list operations that map
    to the underlying tree structure.
    """

    # --- Async list operations --- #

    async def get(self, index: int) -> TreeValueContainer[TreeValueT]:
        """
        Get an item from the list node at the specified index.

        Args:
            index: Index of the item to retrieve

        Returns:
            The item at the specified index

        Raises:
            ObjectIndexError: If the index is out of range
        """
        return await self._storage.list_get(self._path, index, self._txn)

    async def set(self, index: int, value: TreeValueT) -> None:
        """
        Set an item in the list node at the specified index.

        Args:
            index: Index of the item to set
            value: Value to set

        Raises:
            ObjectIndexError: If the index is out of range
        """
        await self._storage.list_set(self._path, index, value, self._txn)

    async def append(self, value: TreeValueT) -> int:
        """
        Append an item to the list node.

        Args:
            value: Value to append

        Returns:
            New length of the list
        """
        return await self._storage.list_append(self._path, value, self._txn)

    async def extend(self, values: list[TreeValueT]) -> int:
        """
        Extend the list node with multiple values.

        Args:
            values: List of values to append

        Returns:
            New length of the list
        """
        return await self._storage.list_extend(self._path, values, self._txn)

    async def insert(self, index: int, value: TreeValueT) -> None:
        """
        Insert an item at a specific position in the list node.

        Args:
            index: Position to insert the value
            value: Value to insert

        Raises:
            ObjectIndexError: If the index is out of range
        """
        await self._storage.list_insert(self._path, index, value, self._txn)

    async def delete(self, index: int) -> None:
        """
        Remove an item from the list node at the specified index.

        Args:
            index: Index of the item to remove

        Raises:
            ObjectIndexError: If the index is out of range
        """
        await self._storage.list_remove(self._path, index, self._txn)

    async def length(self) -> int:
        """
        Get the length of the list node.

        Returns:
            Number of items in the list
        """
        return await self._storage.list_length(self._path, self._txn)

    # --- List conversion and utilities --- #

    async def to_list(self) -> list[TreeValueT]:
        """
        Convert to a regular Python list.

        Returns:
            Python list containing all items from this list node
        """
        return await self._storage.list_to_list(self._path, self._txn)

    async def clear(self) -> None:
        """
        Remove all items from the list node.
        """
        # Get the current length and remove items one by one from the end
        length = await self.length()
        for i in range(length - 1, -1, -1):
            try:
                await self.delete(i)
            except ObjectIndexError:
                # Index became invalid, just skip it
                pass

    async def pop(self, index: int = -1) -> TreeValueContainer[TreeValueT]:
        """
        Remove and return an item from the list node.

        Args:
            index: Index of the item to remove (default: last item)

        Returns:
            The item at the specified index

        Raises:
            ObjectIndexError: If the index is out of range
        """
        value = await self.get(index)
        await self.delete(index)
        return value

    async def __len__(self) -> int:
        """
        Get the number of items in the list node.

        Returns:
            The number of items
        """
        return await self.length()


if TYPE_CHECKING:
    _: type[AsyncTreeListProtocol] = TreeList

```

