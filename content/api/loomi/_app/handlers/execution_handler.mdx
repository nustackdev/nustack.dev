---
title: execution_handler
sidebarTitle: 📦 execution_handler
---

import { Cards, Callout, Tabs } from 'nextra/components'

# loomi._app.handlers.execution_handler 📦

## Classes

<Cards>
  <Cards.Card title="AsyncAppExecutionHandler" href="#asyncappexecutionhandler"/>
  <Cards.Card title="CommonAppExecutionHandler" href="#commonappexecutionhandler"/>
  <Cards.Card title="SyncAppExecutionHandler" href="#syncappexecutionhandler"/>
</Cards>

---

<a id='asyncappexecutionhandler'></a>

## AsyncAppExecutionHandler 🧩

<Callout type='info'>
  Inherits from: `CommonAppExecutionHandler`, `AsyncAppABC`
</Callout>

App feature implementing async execution engine management.

```python filename='class.py'
class AsyncAppExecutionHandler(CommonAppExecutionHandler, AsyncAppABC):
    ...
```

### Class Attributes

| Name | Type | Value | Description |
| ---- | ---- | ----- | ----------- |
| `ex` | `property` | `&lt;property object at 0x107c0a110&gt;` |  |
| `executor` | `property` | `&lt;property object at 0x107b93b00&gt;` |  |

### Properties

| Name | Description |
| ---- | ----------- |
| `e` | Short alias for state adapter. |
| `engine` | Check and return app's state service. |
| `ex` | Short alias for state adapter. |
| `executor` | Check and return app's state service. |
| `key` | Get the unique app instance identifier. |
| `readable_name` | Get a human-readable identifier for the app. |
| `s` | Short alias for state adapter. |
| `state` | Check and return app's state service. |

### Methods

<Tabs items={['add_service_dependency', 'define', 'factory_name', 'get_service_dependency', 'initialize', 'initialize_apps', 'initialize_services', 'shutdown', 'shutdown_apps', 'shutdown_services', 'start']}>
<Tabs.Tab>

### add_service_dependency() 🔧

```python filename='method.py'
def add_service_dependency(self, name: 'str', spec: "'Spec'") -> "'Service'"
```

</Tabs.Tab>

<Tabs.Tab>

### define() 🔧

```python filename='method.py'
def define(self) -> 'OperationProtocol'
```

Define the operation to execute.

</Tabs.Tab>

<Tabs.Tab>

### factory_name() 🔧

```python filename='method.py'
def factory_name(self) -> 'str'
```

Get the fully qualified name of the app class.

**Returns:** `str` - String in format "module.ClassName"

</Tabs.Tab>

<Tabs.Tab>

### get_service_dependency() 🔧

```python filename='method.py'
def get_service_dependency(self, name: 'str') -> "'Service'"
```

Get named dependency if it exists.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `name` | `str` | Dependency name to retrieve | `Required` |

**Returns:** `'Service'` - Dependency service

</Tabs.Tab>

<Tabs.Tab>

### initialize() 🔧

```python filename='method.py'
def initialize(self) -> 'None'
```

Initialize app and its dependencies asynchronously.

</Tabs.Tab>

<Tabs.Tab>

### initialize_apps() 🔧

```python filename='method.py'
def initialize_apps(self) -> 'None'
```

Initialize apps.

</Tabs.Tab>

<Tabs.Tab>

### initialize_services() 🔧

```python filename='method.py'
def initialize_services(self) -> 'None'
```

Initialize app service dependencies asynchronously.

</Tabs.Tab>

<Tabs.Tab>

### shutdown() 🔧

```python filename='method.py'
def shutdown(self) -> 'None'
```

Shutdown app and cleanup dependencies asynchronously.

</Tabs.Tab>

<Tabs.Tab>

### shutdown_apps() 🔧

```python filename='method.py'
def shutdown_apps(self) -> 'None'
```

Shutdown app and cleanup.

</Tabs.Tab>

<Tabs.Tab>

### shutdown_services() 🔧

```python filename='method.py'
def shutdown_services(self) -> 'None'
```

Shutdown app service dependencies asynchronously.

</Tabs.Tab>

<Tabs.Tab>

### start() 🔧

```python filename='method.py'
def start(self, context: 'ContextProtocol | None' = None) -> 'None'
```

Run the app.

</Tabs.Tab>

</Tabs>

### Source Code

```python filename='source.py'
class AsyncAppExecutionHandler(
    CommonAppExecutionHandler[StateT, ExecutorT], AsyncAppABC[StateT, ExecutorT]
):
    """
    App feature implementing async execution engine management.
    """

    @final
    @property
    def executor(self) -> ExecutorT:
        """Check and return app's state service."""
        if "EXECUTOR" not in self._services:
            raise ExecutionError("No execution engine adapter configured")

        executor = cast(ExecutorT, self.get_service_dependency("EXECUTOR"))

        if not executor:
            raise ExecutionError("Execution engine not initialized")

        return executor

    @final
    @property
    def ex(self) -> ExecutorT:
        """Short alias for state adapter."""
        return self.executor

    async def start(self, context: ContextProtocol | None = None) -> None:
        """Run the app."""
        operation_def_fn = getattr(self, "define", None)
        if not operation_def_fn:
            raise ExecutionError("No operation defined")

        operation = None
        if inspect.iscoroutinefunction(operation_def_fn):
            operation = await operation_def_fn()
        elif inspect.ismethod(operation_def_fn):
            operation = operation_def_fn()
        else:
            raise ExecutionError("Operation definition is not a function")

        if inspect.iscoroutinefunction(self.executor.execute):
            await self.executor.execute(operation, context)
        else:
            self.executor.execute(operation, context)

    def define(self) -> OperationProtocol:
        """Define the operation to execute."""
        raise NotImplementedError("Subclasses must implement this method")

```

<a id='commonappexecutionhandler'></a>

## CommonAppExecutionHandler 🧩

<Callout type='info'>
  Inherits from: `AppABC`
</Callout>

Base class for app tasks execution.

```python filename='class.py'
class CommonAppExecutionHandler(AppABC):
    ...
```

### Properties

| Name | Description |
| ---- | ----------- |
| `key` | Get the unique app instance identifier. |
| `readable_name` | Get a human-readable identifier for the app. |

### Methods

<a id='add_service_dependency'></a>

#### add_service_dependency() 🔧

```python filename='method.py'
def add_service_dependency(self, name: 'str', spec: "'Spec'") -> "'Service'"
```

<a id='factory_name'></a>

#### factory_name() 🔧

```python filename='method.py'
def factory_name(self) -> 'str'
```

Get the fully qualified name of the app class.

**Returns:** `str` - String in format "module.ClassName"

<a id='get_service_dependency'></a>

#### get_service_dependency() 🔧

```python filename='method.py'
def get_service_dependency(self, name: 'str') -> "'Service'"
```

Get named dependency if it exists.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `name` | `str` | Dependency name to retrieve | `Required` |

**Returns:** `'Service'` - Dependency service

### Source Code

```python filename='source.py'
class CommonAppExecutionHandler(AppABC[StateT, ExecutorT]):
    """
    Base class for app tasks execution.
    """

    pass

```

<a id='syncappexecutionhandler'></a>

## SyncAppExecutionHandler 🧩

<Callout type='info'>
  Inherits from: `CommonAppExecutionHandler`, `SyncAppABC`
</Callout>

App feature implementing execution engine management.

```python filename='class.py'
class SyncAppExecutionHandler(CommonAppExecutionHandler, SyncAppABC):
    ...
```

### Class Attributes

| Name | Type | Value | Description |
| ---- | ---- | ----- | ----------- |
| `ex` | `property` | `&lt;property object at 0x107c0a250&gt;` |  |
| `executor` | `property` | `&lt;property object at 0x107c0a1b0&gt;` |  |

### Properties

| Name | Description |
| ---- | ----------- |
| `ex` | Short alias for state adapter. |
| `executor` | Check and return app's state service. |
| `key` | Get the unique app instance identifier. |
| `readable_name` | Get a human-readable identifier for the app. |
| `s` | Short alias for state adapter. |
| `state` | Check and return app's state service. |

### Methods

<Tabs items={['add_service_dependency', 'define', 'factory_name', 'get_service_dependency', 'initialize', 'initialize_apps', 'initialize_services', 'shutdown', 'shutdown_apps', 'shutdown_services', 'start']}>
<Tabs.Tab>

### add_service_dependency() 🔧

```python filename='method.py'
def add_service_dependency(self, name: 'str', spec: "'Spec'") -> "'Service'"
```

</Tabs.Tab>

<Tabs.Tab>

### define() 🔧

```python filename='method.py'
def define(self) -> 'OperationProtocol'
```

Define the operation to execute.

</Tabs.Tab>

<Tabs.Tab>

### factory_name() 🔧

```python filename='method.py'
def factory_name(self) -> 'str'
```

Get the fully qualified name of the app class.

**Returns:** `str` - String in format "module.ClassName"

</Tabs.Tab>

<Tabs.Tab>

### get_service_dependency() 🔧

```python filename='method.py'
def get_service_dependency(self, name: 'str') -> "'Service'"
```

Get named dependency if it exists.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `name` | `str` | Dependency name to retrieve | `Required` |

**Returns:** `'Service'` - Dependency service

</Tabs.Tab>

<Tabs.Tab>

### initialize() 🔧

```python filename='method.py'
def initialize(self) -> 'None'
```

Initialize app and its dependencies synchronously.

</Tabs.Tab>

<Tabs.Tab>

### initialize_apps() 🔧

```python filename='method.py'
def initialize_apps(self) -> 'None'
```

Initialize apps.

</Tabs.Tab>

<Tabs.Tab>

### initialize_services() 🔧

```python filename='method.py'
def initialize_services(self) -> 'None'
```

Initialize app service dependencies synchronously.

</Tabs.Tab>

<Tabs.Tab>

### shutdown() 🔧

```python filename='method.py'
def shutdown(self) -> 'None'
```

Shutdown app and cleanup dependencies synchronously.

</Tabs.Tab>

<Tabs.Tab>

### shutdown_apps() 🔧

```python filename='method.py'
def shutdown_apps(self) -> 'None'
```

Shutdown app and cleanup.

</Tabs.Tab>

<Tabs.Tab>

### shutdown_services() 🔧

```python filename='method.py'
def shutdown_services(self) -> 'None'
```

Shutdown app service dependencies synchronously.

</Tabs.Tab>

<Tabs.Tab>

### start() 🔧

```python filename='method.py'
def start(self, context: 'ContextProtocol | None' = None) -> 'None'
```

Run the app.

</Tabs.Tab>

</Tabs>

### Source Code

```python filename='source.py'
class SyncAppExecutionHandler(
    CommonAppExecutionHandler[SyncStateT, SyncExecutorT], SyncAppABC[SyncStateT, SyncExecutorT]
):
    """
    App feature implementing execution engine management.
    """

    @final
    @property
    def executor(self) -> SyncExecutorT:
        """Check and return app's state service."""
        if "EXECUTOR" not in self._services:
            raise ExecutionError("No execution engine adapter configured")

        executor = cast(SyncExecutorT, self.get_service_dependency("EXECUTOR"))

        if not executor:
            raise ExecutionError("Execution engine not initialized")

        return executor

    @final
    @property
    def ex(self) -> SyncExecutorT:
        """Short alias for state adapter."""
        return self.executor

    def start(self, context: ContextProtocol | None = None) -> None:
        """Run the app."""
        operation_def_fn = getattr(self, "define", None)
        if not operation_def_fn:
            raise ExecutionError("No operation defined")

        operation = None
        if inspect.iscoroutinefunction(operation_def_fn):
            raise ExecutionError("Async operation definition not supported")
        elif inspect.ismethod(operation_def_fn):
            operation = operation_def_fn()
        else:
            raise ExecutionError("Operation definition is not a function")

        if inspect.iscoroutinefunction(self.executor.execute):
            raise ExecutionError("Async execution engine not supported")
        elif isinstance(self.executor, SyncExecutorProtocol):
            self.executor.execute(operation, context)

    def define(self) -> OperationProtocol:
        """Define the operation to execute."""
        raise NotImplementedError("Subclasses must implement this method")

```

## Variables & Constants

<Cards>
  <Cards.Card title="ExecutorT" href="#variables"/>
  <Cards.Card title="StateT" href="#variables"/>
  <Cards.Card title="SyncExecutorT" href="#variables"/>
  <Cards.Card title="SyncStateT" href="#variables"/>
  <Cards.Card title="annotations" href="#variables"/>
</Cards>

<a id='variables'></a>

### Variables

| Name | Type | Value | Description |
| ---- | ---- | ----- | ----------- |
| `ExecutorT` | `TypeVar` | `~ExecutorT` |  |
| `StateT` | `TypeVar` | `~StateT` |  |
| `SyncExecutorT` | `TypeVar` | `~SyncExecutorT` |  |
| `SyncStateT` | `TypeVar` | `~SyncStateT` |  |
| `annotations` | `_Feature` | `_Feature((3, 7, 0, &#x27;beta&#x27;, 1), None, 16777216)` |  |

## Source Code

```python filename='source.py'
from __future__ import annotations

import inspect
from typing import cast, final

from loomi.interfaces.executor.context import ContextProtocol
from loomi.interfaces.executor.executor import SyncExecutorProtocol
from loomi.interfaces.executor.operations import OperationProtocol

from ..base import AppABC, AsyncAppABC, SyncAppABC
from ..exceptions import ExecutionError
from ..types import ExecutorT, StateT, SyncExecutorT, SyncStateT


class CommonAppExecutionHandler(AppABC[StateT, ExecutorT]):
    """
    Base class for app tasks execution.
    """

    pass


class AsyncAppExecutionHandler(
    CommonAppExecutionHandler[StateT, ExecutorT], AsyncAppABC[StateT, ExecutorT]
):
    """
    App feature implementing async execution engine management.
    """

    @final
    @property
    def executor(self) -> ExecutorT:
        """Check and return app's state service."""
        if "EXECUTOR" not in self._services:
            raise ExecutionError("No execution engine adapter configured")

        executor = cast(ExecutorT, self.get_service_dependency("EXECUTOR"))

        if not executor:
            raise ExecutionError("Execution engine not initialized")

        return executor

    @final
    @property
    def ex(self) -> ExecutorT:
        """Short alias for state adapter."""
        return self.executor

    async def start(self, context: ContextProtocol | None = None) -> None:
        """Run the app."""
        operation_def_fn = getattr(self, "define", None)
        if not operation_def_fn:
            raise ExecutionError("No operation defined")

        operation = None
        if inspect.iscoroutinefunction(operation_def_fn):
            operation = await operation_def_fn()
        elif inspect.ismethod(operation_def_fn):
            operation = operation_def_fn()
        else:
            raise ExecutionError("Operation definition is not a function")

        if inspect.iscoroutinefunction(self.executor.execute):
            await self.executor.execute(operation, context)
        else:
            self.executor.execute(operation, context)

    def define(self) -> OperationProtocol:
        """Define the operation to execute."""
        raise NotImplementedError("Subclasses must implement this method")


class SyncAppExecutionHandler(
    CommonAppExecutionHandler[SyncStateT, SyncExecutorT], SyncAppABC[SyncStateT, SyncExecutorT]
):
    """
    App feature implementing execution engine management.
    """

    @final
    @property
    def executor(self) -> SyncExecutorT:
        """Check and return app's state service."""
        if "EXECUTOR" not in self._services:
            raise ExecutionError("No execution engine adapter configured")

        executor = cast(SyncExecutorT, self.get_service_dependency("EXECUTOR"))

        if not executor:
            raise ExecutionError("Execution engine not initialized")

        return executor

    @final
    @property
    def ex(self) -> SyncExecutorT:
        """Short alias for state adapter."""
        return self.executor

    def start(self, context: ContextProtocol | None = None) -> None:
        """Run the app."""
        operation_def_fn = getattr(self, "define", None)
        if not operation_def_fn:
            raise ExecutionError("No operation defined")

        operation = None
        if inspect.iscoroutinefunction(operation_def_fn):
            raise ExecutionError("Async operation definition not supported")
        elif inspect.ismethod(operation_def_fn):
            operation = operation_def_fn()
        else:
            raise ExecutionError("Operation definition is not a function")

        if inspect.iscoroutinefunction(self.executor.execute):
            raise ExecutionError("Async execution engine not supported")
        elif isinstance(self.executor, SyncExecutorProtocol):
            self.executor.execute(operation, context)

    def define(self) -> OperationProtocol:
        """Define the operation to execute."""
        raise NotImplementedError("Subclasses must implement this method")

```

