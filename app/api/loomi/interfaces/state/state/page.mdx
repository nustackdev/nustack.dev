---
title: state
sidebarTitle: 📄 state
---

import { Cards, Callout, Tabs } from 'nextra/components'

# 📄 `loomi.interfaces.state.state` Reference

## Classes

<Cards>
  <Cards.Card title="AsyncStateProtocol" href="#asyncstateprotocol"/>
  <Cards.Card title="SyncStateProtocol" href="#syncstateprotocol"/>
</Cards>

---

<a id='asyncstateprotocol'></a>

## AsyncStateProtocol 🧩

<Callout type='info'>
  Inherits from: `Protocol`
</Callout>

Protocol for asynchronous state management.

This interface defines the contract for state storage implementations,
providing methods to access dictionary and list nodes in the state tree,
as well as transaction management.

```python filename='class.py'
class AsyncStateProtocol(Protocol):
    ...
```

### Class Attributes

| Name | Type | Value | Description |
| ---- | ---- | ----- | ----------- |
| `is_async` | `property` | `&lt;property object at 0x10f4f8b30&gt;` |  |

### Properties

| Name | Description |
| ---- | ----------- |
| `is_async` | Check if the protocol is asynchronous. |

### Methods

<Tabs items={['begin_transaction', 'dict', 'exists', 'is_dict', 'is_list', 'list', 'subscribe', 'transaction', 'unsubscribe']}>
<Tabs.Tab>

### begin_transaction() 🔧

```python filename='method.py'
def begin_transaction(self) -> "'AsyncTransactionProtocol[StateValueT]'"
```

Begin a new transaction.

**Returns:** `'AsyncTransactionProtocol[StateValueT]'` - New transaction instance

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `TransactionError` | If transaction cannot be started |

</Tabs.Tab>

<Tabs.Tab>

### dict() 🔧

```python filename='method.py'
def dict(self, path: 'StatePathComponent', /, *paths: 'StatePathComponent', txn: "'AsyncTransactionProtocol[StateValueT] | None'" = None) -> 'AsyncTreeDictProtocol[StateValueT]'
```

Get a nested dictionary node interface.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `StatePathComponent` | First path segment *paths: Additional path segments | `Required` |
| `txn` | `'AsyncTransactionProtocol[StateValueT] | None'` | Optional transaction to use | `None` |

**Returns:** `AsyncTreeDictProtocol[StateValueT]` - A new AsyncTreeDictProtocol instance for the nested dictionary node

</Tabs.Tab>

<Tabs.Tab>

### exists() 🔧

```python filename='method.py'
def exists(self, path: 'StatePathComponent', /, *paths: 'StatePathComponent', txn: "'AsyncTransactionProtocol[StateValueT] | None'" = None) -> 'bool'
```

Check if the path exists.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `StatePathComponent` | First path segment *paths: Additional path segments | `Required` |
| `txn` | `'AsyncTransactionProtocol[StateValueT] | None'` | Optional transaction to use | `None` |

**Returns:** `bool` - True if the path exists, False otherwise

</Tabs.Tab>

<Tabs.Tab>

### is_dict() 🔧

```python filename='method.py'
def is_dict(self, path: 'StatePathComponent', /, *paths: 'StatePathComponent', txn: "'AsyncTransactionProtocol[StateValueT] | None'" = None) -> 'bool'
```

Check if the path is a dictionary node.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `StatePathComponent` | First path segment *paths: Additional path segments | `Required` |
| `txn` | `'AsyncTransactionProtocol[StateValueT] | None'` | Optional transaction to use | `None` |

**Returns:** `bool` - True if the path is a dictionary node, False otherwise

</Tabs.Tab>

<Tabs.Tab>

### is_list() 🔧

```python filename='method.py'
def is_list(self, path: 'StatePathComponent', /, *paths: 'StatePathComponent', txn: "'AsyncTransactionProtocol[StateValueT] | None'" = None) -> 'bool'
```

Check if the path is a list node.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `StatePathComponent` | First path segment *paths: Additional path segments | `Required` |
| `txn` | `'AsyncTransactionProtocol[StateValueT] | None'` | Optional transaction to use | `None` |

**Returns:** `bool` - True if the path is a list node, False otherwise

</Tabs.Tab>

<Tabs.Tab>

### list() 🔧

```python filename='method.py'
def list(self, path: 'StatePathComponent', /, *paths: 'StatePathComponent', txn: "'AsyncTransactionProtocol[StateValueT] | None'" = None) -> 'AsyncTreeListProtocol[StateValueT]'
```

Get a nested list node interface.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `StatePathComponent` | First path segment *paths: Additional path segments | `Required` |
| `txn` | `'AsyncTransactionProtocol[StateValueT] | None'` | Optional transaction to use | `None` |

**Returns:** `AsyncTreeListProtocol[StateValueT]` - A new AsyncTreeListProtocol instance for the nested list node

</Tabs.Tab>

<Tabs.Tab>

### subscribe() 🔧

```python filename='method.py'
def subscribe(self, key: 'StatePath', callback: 'AsyncCallbackFn', depth: 'int' = Ellipsis) -> 'AsyncSubscriptionProtocol'
```

Subscribe to changes under key prefix.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `key` | `StatePath` | Key prefix to subscribe to | `Required` |
| `callback` | `AsyncCallbackFn` | Async callback function for notifications | `Required` |
| `depth` | `int` | Depth of topic pattern matching (default: 0 for exact match) If set to 0, matches exact topic; if set to 1, matches prefix; if set to -1, matches all subtopics. | `Ellipsis` |

**Returns:** `AsyncSubscriptionProtocol` - Subscription object for unsubscribing

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObserverError` | If subscription fails |

</Tabs.Tab>

<Tabs.Tab>

### transaction() 🔧

```python filename='method.py'
def transaction(self) -> 'AsyncTransactionContextManagerProtocol[StateValueT]'
```

Get transaction context manager.

**Returns:** `AsyncTransactionContextManagerProtocol[StateValueT]` - Transaction context manager for use in async with statements

</Tabs.Tab>

<Tabs.Tab>

### unsubscribe() 🔧

```python filename='method.py'
def unsubscribe(self, subscription: 'AsyncSubscriptionProtocol') -> 'None'
```

Unsubscribe from changes under key prefix.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `subscription` | `AsyncSubscriptionProtocol` | Subscription to cancel | `Required` |

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObserverError` | If unsubscribe fails |

</Tabs.Tab>

</Tabs>

### Source Code

```python filename='source.py'
@runtime_checkable
class AsyncStateProtocol(Protocol[StateValueT]):
    """
    Protocol for asynchronous state management.

    This interface defines the contract for state storage implementations,
    providing methods to access dictionary and list nodes in the state tree,
    as well as transaction management.
    """

    @property
    def is_async(self) -> bool:
        """
        Check if the protocol is asynchronous.

        Returns:
            True if the protocol is asynchronous, False otherwise
        """
        return True

    async def dict(
        self,
        path: StatePathComponent,
        /,
        *paths: StatePathComponent,
        txn: "AsyncTransactionProtocol[StateValueT] | None" = None,
    ) -> AsyncTreeDictProtocol[StateValueT]:
        """
        Get a nested dictionary node interface.

        Args:
            path: First path segment
            *paths: Additional path segments
            txn: Optional transaction to use

        Returns:
            A new AsyncTreeDictProtocol instance for the nested dictionary node
        """
        ...

    async def list(
        self,
        path: StatePathComponent,
        /,
        *paths: StatePathComponent,
        txn: "AsyncTransactionProtocol[StateValueT] | None" = None,
    ) -> AsyncTreeListProtocol[StateValueT]:
        """
        Get a nested list node interface.

        Args:
            path: First path segment
            *paths: Additional path segments
            txn: Optional transaction to use

        Returns:
            A new AsyncTreeListProtocol instance for the nested list node
        """
        ...

    async def is_dict(
        self,
        path: StatePathComponent,
        /,
        *paths: StatePathComponent,
        txn: "AsyncTransactionProtocol[StateValueT] | None" = None,
    ) -> bool:
        """
        Check if the path is a dictionary node.

        Args:
            path: First path segment
            *paths: Additional path segments
            txn: Optional transaction to use

        Returns:
            True if the path is a dictionary node, False otherwise
        """
        ...

    async def is_list(
        self,
        path: StatePathComponent,
        /,
        *paths: StatePathComponent,
        txn: "AsyncTransactionProtocol[StateValueT] | None" = None,
    ) -> bool:
        """
        Check if the path is a list node.

        Args:
            path: First path segment
            *paths: Additional path segments
            txn: Optional transaction to use

        Returns:
            True if the path is a list node, False otherwise
        """
        ...

    async def exists(
        self,
        path: StatePathComponent,
        /,
        *paths: StatePathComponent,
        txn: "AsyncTransactionProtocol[StateValueT] | None" = None,
    ) -> bool:
        """
        Check if the path exists.

        Args:
            path: First path segment
            *paths: Additional path segments
            txn: Optional transaction to use

        Returns:
            True if the path exists, False otherwise
        """
        ...

    async def begin_transaction(self) -> "AsyncTransactionProtocol[StateValueT]":
        """
        Begin a new transaction.

        Returns:
            New transaction instance

        Raises:
            TransactionError: If transaction cannot be started
        """
        ...

    async def transaction(self) -> AsyncTransactionContextManagerProtocol[StateValueT]:
        """
        Get transaction context manager.

        Returns:
            Transaction context manager for use in async with statements
        """
        ...

    async def subscribe(
        self,
        key: StatePath,
        callback: AsyncCallbackFn,
        depth: int = ...,
    ) -> AsyncSubscriptionProtocol:
        """
        Subscribe to changes under key prefix.

        Args:
            key: Key prefix to subscribe to
            callback: Async callback function for notifications
            depth: Depth of topic pattern matching (default: 0 for exact match)
                If set to 0, matches exact topic; if set to 1, matches prefix; if set to -1, matches all subtopics.


        Returns:
            Subscription object for unsubscribing

        Raises:
            ObserverError: If subscription fails
        """
        ...

    async def unsubscribe(self, subscription: AsyncSubscriptionProtocol) -> None:
        """
        Unsubscribe from changes under key prefix.

        Args:
            subscription: Subscription to cancel

        Raises:
            ObserverError: If unsubscribe fails
        """
        ...

```

<a id='syncstateprotocol'></a>

## SyncStateProtocol 🧩

<Callout type='info'>
  Inherits from: `Protocol`
</Callout>

Protocol for synchronous state management.

This interface defines the contract for state storage implementations,
providing methods to access dictionary and list nodes in the state tree,
as well as transaction management.

```python filename='class.py'
class SyncStateProtocol(Protocol):
    ...
```

### Class Attributes

| Name | Type | Value | Description |
| ---- | ---- | ----- | ----------- |
| `is_sync` | `property` | `&lt;property object at 0x10f5160c0&gt;` |  |

### Properties

| Name | Description |
| ---- | ----------- |
| `is_sync` | Check if the protocol is synchronous. |

### Methods

<Tabs items={['begin_transaction', 'dict', 'exists', 'is_dict', 'is_list', 'list', 'subscribe', 'transaction', 'unsubscribe']}>
<Tabs.Tab>

### begin_transaction() 🔧

```python filename='method.py'
def begin_transaction(self) -> "'SyncTransactionProtocol[StateValueT]'"
```

Begin a new transaction.

**Returns:** `'SyncTransactionProtocol[StateValueT]'` - New transaction instance

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `TransactionError` | If transaction cannot be started |

</Tabs.Tab>

<Tabs.Tab>

### dict() 🔧

```python filename='method.py'
def dict(self, path: 'StatePathComponent', /, *paths: 'StatePathComponent', txn: "'SyncTransactionProtocol[StateValueT] | None'" = None) -> 'SyncTreeDictProtocol[StateValueT]'
```

Get a nested dictionary node interface.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `StatePathComponent` | First path segment *paths: Additional path segments | `Required` |
| `txn` | `'SyncTransactionProtocol[StateValueT] | None'` | Optional transaction to use | `None` |

**Returns:** `SyncTreeDictProtocol[StateValueT]` - A new SyncTreeDictProtocol instance for the nested dictionary node

</Tabs.Tab>

<Tabs.Tab>

### exists() 🔧

```python filename='method.py'
def exists(self, path: 'StatePathComponent', /, *paths: 'StatePathComponent', txn: "'AsyncTransactionProtocol[StateValueT] | None'" = None) -> 'bool'
```

Check if the path exists.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `StatePathComponent` | First path segment *paths: Additional path segments | `Required` |
| `txn` | `'AsyncTransactionProtocol[StateValueT] | None'` | Optional transaction to use | `None` |

**Returns:** `bool` - True if the path exists, False otherwise

</Tabs.Tab>

<Tabs.Tab>

### is_dict() 🔧

```python filename='method.py'
def is_dict(self, path: 'StatePathComponent', /, *paths: 'StatePathComponent', txn: "'AsyncTransactionProtocol[StateValueT] | None'" = None) -> 'bool'
```

Check if the path is a dictionary node.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `StatePathComponent` | First path segment *paths: Additional path segments | `Required` |
| `txn` | `'AsyncTransactionProtocol[StateValueT] | None'` | Optional transaction to use | `None` |

**Returns:** `bool` - True if the path is a dictionary node, False otherwise

</Tabs.Tab>

<Tabs.Tab>

### is_list() 🔧

```python filename='method.py'
def is_list(self, path: 'StatePathComponent', /, *paths: 'StatePathComponent', txn: "'AsyncTransactionProtocol[StateValueT] | None'" = None) -> 'bool'
```

Check if the path is a list node.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `StatePathComponent` | First path segment *paths: Additional path segments | `Required` |
| `txn` | `'AsyncTransactionProtocol[StateValueT] | None'` | Optional transaction to use | `None` |

**Returns:** `bool` - True if the path is a list node, False otherwise

</Tabs.Tab>

<Tabs.Tab>

### list() 🔧

```python filename='method.py'
def list(self, path: 'StatePathComponent', /, *paths: 'StatePathComponent', txn: "'SyncTransactionProtocol[StateValueT] | None'" = None) -> 'SyncTreeListProtocol[StateValueT]'
```

Get a nested list node interface.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `path` | `StatePathComponent` | First path segment *paths: Additional path segments | `Required` |
| `txn` | `'SyncTransactionProtocol[StateValueT] | None'` | Optional transaction to use | `None` |

**Returns:** `SyncTreeListProtocol[StateValueT]` - A new SyncTreeListProtocol instance for the nested list node

</Tabs.Tab>

<Tabs.Tab>

### subscribe() 🔧

```python filename='method.py'
def subscribe(self, key: 'StatePath', callback: 'SyncCallbackFn', depth: 'int' = Ellipsis) -> 'SyncSubscriptionProtocol'
```

Subscribe to changes under key prefix.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `key` | `StatePath` | Key prefix to subscribe to | `Required` |
| `callback` | `SyncCallbackFn` | Callback function for notifications | `Required` |
| `depth` | `int` | Depth of topic pattern matching (default: 0 for exact match) If set to 0, matches exact topic; if set to 1, matches prefix; if set to -1, matches all subtopics. | `Ellipsis` |

**Returns:** `SyncSubscriptionProtocol` - Subscription object for unsubscribing

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObserverError` | If subscription fails |

</Tabs.Tab>

<Tabs.Tab>

### transaction() 🔧

```python filename='method.py'
def transaction(self) -> 'SyncTransactionContextManagerProtocol[StateValueT]'
```

Get transaction context manager.

**Returns:** `SyncTransactionContextManagerProtocol[StateValueT]` - Transaction context manager for use in with statements

</Tabs.Tab>

<Tabs.Tab>

### unsubscribe() 🔧

```python filename='method.py'
def unsubscribe(self, subscription: 'SyncSubscriptionProtocol') -> 'None'
```

Unsubscribe from changes under key prefix.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `subscription` | `SyncSubscriptionProtocol` | Subscription to cancel | `Required` |

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ObserverError` | If unsubscribe fails |

</Tabs.Tab>

</Tabs>

### Source Code

```python filename='source.py'
@runtime_checkable
class SyncStateProtocol(Protocol[StateValueT]):
    """
    Protocol for synchronous state management.

    This interface defines the contract for state storage implementations,
    providing methods to access dictionary and list nodes in the state tree,
    as well as transaction management.
    """

    @property
    def is_sync(self) -> bool:
        """
        Check if the protocol is synchronous.

        Returns:
            True if the protocol is synchronous, False otherwise
        """
        return True

    def dict(
        self,
        path: StatePathComponent,
        /,
        *paths: StatePathComponent,
        txn: "SyncTransactionProtocol[StateValueT] | None" = None,
    ) -> SyncTreeDictProtocol[StateValueT]:
        """
        Get a nested dictionary node interface.

        Args:
            path: First path segment
            *paths: Additional path segments
            txn: Optional transaction to use

        Returns:
            A new SyncTreeDictProtocol instance for the nested dictionary node
        """
        ...

    def list(
        self,
        path: StatePathComponent,
        /,
        *paths: StatePathComponent,
        txn: "SyncTransactionProtocol[StateValueT] | None" = None,
    ) -> SyncTreeListProtocol[StateValueT]:
        """
        Get a nested list node interface.

        Args:
            path: First path segment
            *paths: Additional path segments
            txn: Optional transaction to use

        Returns:
            A new SyncTreeListProtocol instance for the nested list node
        """
        ...

    def is_dict(
        self,
        path: StatePathComponent,
        /,
        *paths: StatePathComponent,
        txn: "AsyncTransactionProtocol[StateValueT] | None" = None,
    ) -> bool:
        """
        Check if the path is a dictionary node.

        Args:
            path: First path segment
            *paths: Additional path segments
            txn: Optional transaction to use

        Returns:
            True if the path is a dictionary node, False otherwise
        """
        ...

    def is_list(
        self,
        path: StatePathComponent,
        /,
        *paths: StatePathComponent,
        txn: "AsyncTransactionProtocol[StateValueT] | None" = None,
    ) -> bool:
        """
        Check if the path is a list node.

        Args:
            path: First path segment
            *paths: Additional path segments
            txn: Optional transaction to use

        Returns:
            True if the path is a list node, False otherwise
        """
        ...

    def exists(
        self,
        path: StatePathComponent,
        /,
        *paths: StatePathComponent,
        txn: "AsyncTransactionProtocol[StateValueT] | None" = None,
    ) -> bool:
        """
        Check if the path exists.

        Args:
            path: First path segment
            *paths: Additional path segments
            txn: Optional transaction to use

        Returns:
            True if the path exists, False otherwise
        """
        ...

    def begin_transaction(self) -> "SyncTransactionProtocol[StateValueT]":
        """
        Begin a new transaction.

        Returns:
            New transaction instance

        Raises:
            TransactionError: If transaction cannot be started
        """
        ...

    def transaction(self) -> SyncTransactionContextManagerProtocol[StateValueT]:
        """
        Get transaction context manager.

        Returns:
            Transaction context manager for use in with statements
        """
        ...

    def subscribe(
        self,
        key: StatePath,
        callback: SyncCallbackFn,
        depth: int = ...,
    ) -> SyncSubscriptionProtocol:
        """
        Subscribe to changes under key prefix.

        Args:
            key: Key prefix to subscribe to
            callback: Callback function for notifications
            depth: Depth of topic pattern matching (default: 0 for exact match)
                If set to 0, matches exact topic; if set to 1, matches prefix; if set to -1, matches all subtopics.


        Returns:
            Subscription object for unsubscribing

        Raises:
            ObserverError: If subscription fails
        """
        ...

    def unsubscribe(self, subscription: SyncSubscriptionProtocol) -> None:
        """
        Unsubscribe from changes under key prefix.

        Args:
            subscription: Subscription to cancel

        Raises:
            ObserverError: If unsubscribe fails
        """
        ...

```

## Source Code

```python filename='source.py'
from __future__ import annotations

from typing import Protocol, runtime_checkable

from .kv import (
    AsyncTransactionContextManagerProtocol,
    AsyncTransactionProtocol,
    SyncTransactionContextManagerProtocol,
    SyncTransactionProtocol,
)
from .observer import AsyncSubscriptionProtocol, SyncSubscriptionProtocol
from .tree import (
    AsyncTreeDictProtocol,
    AsyncTreeListProtocol,
    SyncTreeDictProtocol,
    SyncTreeListProtocol,
)
from .type_vars import StateValueT
from .types import AsyncCallbackFn, StatePath, StatePathComponent, SyncCallbackFn

__all__ = [
    "AsyncStateProtocol",
    "SyncStateProtocol",
]


@runtime_checkable
class AsyncStateProtocol(Protocol[StateValueT]):
    """
    Protocol for asynchronous state management.

    This interface defines the contract for state storage implementations,
    providing methods to access dictionary and list nodes in the state tree,
    as well as transaction management.
    """

    @property
    def is_async(self) -> bool:
        """
        Check if the protocol is asynchronous.

        Returns:
            True if the protocol is asynchronous, False otherwise
        """
        return True

    async def dict(
        self,
        path: StatePathComponent,
        /,
        *paths: StatePathComponent,
        txn: "AsyncTransactionProtocol[StateValueT] | None" = None,
    ) -> AsyncTreeDictProtocol[StateValueT]:
        """
        Get a nested dictionary node interface.

        Args:
            path: First path segment
            *paths: Additional path segments
            txn: Optional transaction to use

        Returns:
            A new AsyncTreeDictProtocol instance for the nested dictionary node
        """
        ...

    async def list(
        self,
        path: StatePathComponent,
        /,
        *paths: StatePathComponent,
        txn: "AsyncTransactionProtocol[StateValueT] | None" = None,
    ) -> AsyncTreeListProtocol[StateValueT]:
        """
        Get a nested list node interface.

        Args:
            path: First path segment
            *paths: Additional path segments
            txn: Optional transaction to use

        Returns:
            A new AsyncTreeListProtocol instance for the nested list node
        """
        ...

    async def is_dict(
        self,
        path: StatePathComponent,
        /,
        *paths: StatePathComponent,
        txn: "AsyncTransactionProtocol[StateValueT] | None" = None,
    ) -> bool:
        """
        Check if the path is a dictionary node.

        Args:
            path: First path segment
            *paths: Additional path segments
            txn: Optional transaction to use

        Returns:
            True if the path is a dictionary node, False otherwise
        """
        ...

    async def is_list(
        self,
        path: StatePathComponent,
        /,
        *paths: StatePathComponent,
        txn: "AsyncTransactionProtocol[StateValueT] | None" = None,
    ) -> bool:
        """
        Check if the path is a list node.

        Args:
            path: First path segment
            *paths: Additional path segments
            txn: Optional transaction to use

        Returns:
            True if the path is a list node, False otherwise
        """
        ...

    async def exists(
        self,
        path: StatePathComponent,
        /,
        *paths: StatePathComponent,
        txn: "AsyncTransactionProtocol[StateValueT] | None" = None,
    ) -> bool:
        """
        Check if the path exists.

        Args:
            path: First path segment
            *paths: Additional path segments
            txn: Optional transaction to use

        Returns:
            True if the path exists, False otherwise
        """
        ...

    async def begin_transaction(self) -> "AsyncTransactionProtocol[StateValueT]":
        """
        Begin a new transaction.

        Returns:
            New transaction instance

        Raises:
            TransactionError: If transaction cannot be started
        """
        ...

    async def transaction(self) -> AsyncTransactionContextManagerProtocol[StateValueT]:
        """
        Get transaction context manager.

        Returns:
            Transaction context manager for use in async with statements
        """
        ...

    async def subscribe(
        self,
        key: StatePath,
        callback: AsyncCallbackFn,
        depth: int = ...,
    ) -> AsyncSubscriptionProtocol:
        """
        Subscribe to changes under key prefix.

        Args:
            key: Key prefix to subscribe to
            callback: Async callback function for notifications
            depth: Depth of topic pattern matching (default: 0 for exact match)
                If set to 0, matches exact topic; if set to 1, matches prefix; if set to -1, matches all subtopics.


        Returns:
            Subscription object for unsubscribing

        Raises:
            ObserverError: If subscription fails
        """
        ...

    async def unsubscribe(self, subscription: AsyncSubscriptionProtocol) -> None:
        """
        Unsubscribe from changes under key prefix.

        Args:
            subscription: Subscription to cancel

        Raises:
            ObserverError: If unsubscribe fails
        """
        ...


@runtime_checkable
class SyncStateProtocol(Protocol[StateValueT]):
    """
    Protocol for synchronous state management.

    This interface defines the contract for state storage implementations,
    providing methods to access dictionary and list nodes in the state tree,
    as well as transaction management.
    """

    @property
    def is_sync(self) -> bool:
        """
        Check if the protocol is synchronous.

        Returns:
            True if the protocol is synchronous, False otherwise
        """
        return True

    def dict(
        self,
        path: StatePathComponent,
        /,
        *paths: StatePathComponent,
        txn: "SyncTransactionProtocol[StateValueT] | None" = None,
    ) -> SyncTreeDictProtocol[StateValueT]:
        """
        Get a nested dictionary node interface.

        Args:
            path: First path segment
            *paths: Additional path segments
            txn: Optional transaction to use

        Returns:
            A new SyncTreeDictProtocol instance for the nested dictionary node
        """
        ...

    def list(
        self,
        path: StatePathComponent,
        /,
        *paths: StatePathComponent,
        txn: "SyncTransactionProtocol[StateValueT] | None" = None,
    ) -> SyncTreeListProtocol[StateValueT]:
        """
        Get a nested list node interface.

        Args:
            path: First path segment
            *paths: Additional path segments
            txn: Optional transaction to use

        Returns:
            A new SyncTreeListProtocol instance for the nested list node
        """
        ...

    def is_dict(
        self,
        path: StatePathComponent,
        /,
        *paths: StatePathComponent,
        txn: "AsyncTransactionProtocol[StateValueT] | None" = None,
    ) -> bool:
        """
        Check if the path is a dictionary node.

        Args:
            path: First path segment
            *paths: Additional path segments
            txn: Optional transaction to use

        Returns:
            True if the path is a dictionary node, False otherwise
        """
        ...

    def is_list(
        self,
        path: StatePathComponent,
        /,
        *paths: StatePathComponent,
        txn: "AsyncTransactionProtocol[StateValueT] | None" = None,
    ) -> bool:
        """
        Check if the path is a list node.

        Args:
            path: First path segment
            *paths: Additional path segments
            txn: Optional transaction to use

        Returns:
            True if the path is a list node, False otherwise
        """
        ...

    def exists(
        self,
        path: StatePathComponent,
        /,
        *paths: StatePathComponent,
        txn: "AsyncTransactionProtocol[StateValueT] | None" = None,
    ) -> bool:
        """
        Check if the path exists.

        Args:
            path: First path segment
            *paths: Additional path segments
            txn: Optional transaction to use

        Returns:
            True if the path exists, False otherwise
        """
        ...

    def begin_transaction(self) -> "SyncTransactionProtocol[StateValueT]":
        """
        Begin a new transaction.

        Returns:
            New transaction instance

        Raises:
            TransactionError: If transaction cannot be started
        """
        ...

    def transaction(self) -> SyncTransactionContextManagerProtocol[StateValueT]:
        """
        Get transaction context manager.

        Returns:
            Transaction context manager for use in with statements
        """
        ...

    def subscribe(
        self,
        key: StatePath,
        callback: SyncCallbackFn,
        depth: int = ...,
    ) -> SyncSubscriptionProtocol:
        """
        Subscribe to changes under key prefix.

        Args:
            key: Key prefix to subscribe to
            callback: Callback function for notifications
            depth: Depth of topic pattern matching (default: 0 for exact match)
                If set to 0, matches exact topic; if set to 1, matches prefix; if set to -1, matches all subtopics.


        Returns:
            Subscription object for unsubscribing

        Raises:
            ObserverError: If subscription fails
        """
        ...

    def unsubscribe(self, subscription: SyncSubscriptionProtocol) -> None:
        """
        Unsubscribe from changes under key prefix.

        Args:
            subscription: Subscription to cancel

        Raises:
            ObserverError: If unsubscribe fails
        """
        ...

```

