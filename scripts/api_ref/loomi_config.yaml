# pydoc-mdx Configuration File

# Metadata settings
meta:
  name: "loomi"
  version: "0.2.0"  # Version of the package

# Discovery settings
discovery:
  # Path to your package
  source_dir: "src"
  
  # Recursively discover modules in subdirectories
  recursive: true
  
  # Modules to exclude from documentation
  exclude_modules:
    - "loomi._utils"
    - "loomi._lib"
    - "loomi._logging"
  
  # Patterns to exclude modules matching these regex patterns
  exclude_patterns:
    - '^(?:.+\.)*\_?logger$'  # Skip loggers
    - '^(?:.+\.)*\_?types$'  # Skip types
    - '^(?:.+\.)*\_?type_vars$'  # Skip type_vars

  # Specific module configurations
  modules:
    - name: "loomi"
      title: "Loomi"  # Custom title
      show_private: true  # Include private members in this module
      show_dunder: true  # Exclude dunder methods
    

# Rendering settings
rendering:
  # Include private members (starting with _)
  show_private: true
  
  # Include dunder methods (like __str__)
  show_dunder: true
  
  # Include source code in the documentation
  include_source_code: true
  
  # Order of sections in the documentation
  section_order:
    - "Classes"
    - "Functions"
    - "Variables"
    - "Modules"
  
  # Emoji mapping for different types
  emojis:
    package: "📦"
    module: "📄"
    class: "🧩"
    function: "⚙️"
    method: "🔧"
    property: "🔑"
    attribute: "🏷️"
    variable: "🔢"
    constant: "🔒"

# Output settings
output:
  # Path where to output MDX files
  output_dir: "docs/app/api"
  
  # Filename for metadata files (used by Nextra)
  meta_filename: "_meta.json"
  
  # Whether to create index.mdx files for directories
  create_index: true
  
  # Whether to overwrite existing files
  overwrite: true