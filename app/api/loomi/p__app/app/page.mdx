---
title: app
sidebarTitle: 📄 app
---

import { Cards, Callout, Tabs } from 'nextra/components'

# 📄 `loomi._app.app` Reference

<Callout>
Apps for synchronous and asynchronous functionality.
</Callout>

Apps are constructed by composing various components, including initialization,
execution handling, service handling, and state management.
The `AsyncApp` class is designed for asynchronous applications, while the `SyncApp` class
is designed for synchronous applications.

The `AsyncApp` class inherits from:
- `AsyncAppInitializer`: Handles asynchronous app initialization.
- `AsyncCommonAppStateHandler`: Manages the app's state asynchronously.
- `AsyncAppExecutionHandler`: Handles asynchronous execution of app functions.
- `AsyncAppServicesHandler`: Manages asynchronous service handling.

The `SyncApp` class inherits from:
- `SyncAppInitializer`: Handles synchronous app initialization.
- `SyncCommonAppStateHandler`: Manages the app's state synchronously.
- `SyncAppExecutionHandler`: Handles synchronous execution of app functions.
- `SyncAppServicesHandler`: Manages synchronous service handling.

## Classes

<Cards>
  <Cards.Card title="AsyncApp" href="#asyncapp"/>
  <Cards.Card title="SyncApp" href="#syncapp"/>
</Cards>

---

<a id='asyncapp'></a>

## AsyncApp 🧩

<Callout type='info'>
  Inherits from: `AsyncAppInitializer`, `AsyncCommonAppStateHandler`, `AsyncAppExecutionHandler`, `AsyncAppServicesHandler`, `AsyncAppComposer`
</Callout>

Implementation of app initialization and lifecycle management.

This mixin provides implementations for app initialization and shutdown.

```python filename='class.py'
class AsyncApp(AsyncAppInitializer, AsyncCommonAppStateHandler, AsyncAppExecutionHandler, AsyncAppServicesHandler, AsyncAppComposer):
    ...
```

### Properties

| Name | Description |
| ---- | ----------- |
| `e` | Short alias for state adapter. |
| `engine` | Check and return app's state service. |
| `ex` | Short alias for state adapter. |
| `executor` | Check and return app's state service. |
| `key` | Get the unique app instance identifier. |
| `readable_name` | Get a human-readable identifier for the app. |
| `s` | Short alias for state adapter. |
| `state` | Check and return app's state service. |

### Methods

<Tabs items={['add_service_dependency', 'define', 'factory_name', 'get_service_dependency', 'initialize', 'initialize_apps', 'initialize_services', 'shutdown', 'shutdown_apps', 'shutdown_services', 'start']}>
<Tabs.Tab>

### add_service_dependency() 🔧

```python filename='method.py'
def add_service_dependency(self, name: 'str', spec: "'Spec'") -> "'Service'"
```

Add service dependency.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `name` | `str` | Dependency name | `Required` |
| `spec` | `'Spec'` | Spec of the service | `Required` |

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `DependencyError` | If dependency invalid or creates cycle |

</Tabs.Tab>

<Tabs.Tab>

### define() 🔧

```python filename='method.py'
def define(self) -> 'OperationProtocol'
```

Define the operation to execute.

</Tabs.Tab>

<Tabs.Tab>

### factory_name() 🔧

```python filename='method.py'
def factory_name(self) -> 'str'
```

Get the fully qualified name of the app class.

**Returns:** `str` - String in format "module.ClassName"

</Tabs.Tab>

<Tabs.Tab>

### get_service_dependency() 🔧

```python filename='method.py'
def get_service_dependency(self, name: 'str') -> "'Service'"
```

Get named dependency if it exists.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `name` | `str` | Dependency name to retrieve | `Required` |

**Returns:** `'Service'` - Dependency service

</Tabs.Tab>

<Tabs.Tab>

### initialize() 🔧

```python filename='method.py'
def initialize(self) -> 'None'
```

Initialize app.

</Tabs.Tab>

<Tabs.Tab>

### initialize_apps() 🔧

```python filename='method.py'
def initialize_apps(self)
```

Initialize apps.

</Tabs.Tab>

<Tabs.Tab>

### initialize_services() 🔧

```python filename='method.py'
def initialize_services(self)
```

Initialize app service dependencies asynchronously.

</Tabs.Tab>

<Tabs.Tab>

### shutdown() 🔧

```python filename='method.py'
def shutdown(self) -> 'None'
```

Shutdown app and cleanup.

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ShutdownError` | If shutdown fails |

</Tabs.Tab>

<Tabs.Tab>

### shutdown_apps() 🔧

```python filename='method.py'
def shutdown_apps(self)
```

Shutdown app and cleanup.

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ShutdownError` | If shutdown fails |

</Tabs.Tab>

<Tabs.Tab>

### shutdown_services() 🔧

```python filename='method.py'
def shutdown_services(self)
```

Shutdown app service dependencies asynchronously.

</Tabs.Tab>

<Tabs.Tab>

### start() 🔧

```python filename='method.py'
def start(self, context: 'ContextProtocol | None' = None) -> 'None'
```

Run the app.

</Tabs.Tab>

</Tabs>

### Source Code

```python filename='source.py'
class AsyncApp(
    AsyncAppInitializer[StateT, ExecutorT],
    AsyncCommonAppStateHandler[StateT, ExecutorT],
    AsyncAppExecutionHandler[StateT, ExecutorT],
    AsyncAppServicesHandler[StateT, ExecutorT],
    AsyncAppComposer[StateT, ExecutorT],
):
    pass

```

<a id='syncapp'></a>

## SyncApp 🧩

<Callout type='info'>
  Inherits from: `SyncAppInitializer`, `SyncCommonAppStateHandler`, `SyncAppExecutionHandler`, `SyncAppServicesHandler`, `SyncAppComposer`
</Callout>

Implementation of app initialization and lifecycle management.

This mixin provides implementations for app initialization and shutdown.

```python filename='class.py'
class SyncApp(SyncAppInitializer, SyncCommonAppStateHandler, SyncAppExecutionHandler, SyncAppServicesHandler, SyncAppComposer):
    ...
```

### Properties

| Name | Description |
| ---- | ----------- |
| `ex` | Short alias for state adapter. |
| `executor` | Check and return app's state service. |
| `key` | Get the unique app instance identifier. |
| `readable_name` | Get a human-readable identifier for the app. |
| `s` | Short alias for state adapter. |
| `state` | Check and return app's state service. |

### Methods

<Tabs items={['add_service_dependency', 'define', 'factory_name', 'get_service_dependency', 'initialize', 'initialize_apps', 'initialize_services', 'shutdown', 'shutdown_apps', 'shutdown_services', 'start']}>
<Tabs.Tab>

### add_service_dependency() 🔧

```python filename='method.py'
def add_service_dependency(self, name: 'str', spec: "'Spec'") -> "'Service'"
```

Add service dependency.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `name` | `str` | Dependency name | `Required` |
| `spec` | `'Spec'` | Spec of the service | `Required` |

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `DependencyError` | If dependency invalid or creates cycle |

</Tabs.Tab>

<Tabs.Tab>

### define() 🔧

```python filename='method.py'
def define(self) -> 'OperationProtocol'
```

Define the operation to execute.

</Tabs.Tab>

<Tabs.Tab>

### factory_name() 🔧

```python filename='method.py'
def factory_name(self) -> 'str'
```

Get the fully qualified name of the app class.

**Returns:** `str` - String in format "module.ClassName"

</Tabs.Tab>

<Tabs.Tab>

### get_service_dependency() 🔧

```python filename='method.py'
def get_service_dependency(self, name: 'str') -> "'Service'"
```

Get named dependency if it exists.

**Parameters:**

| Name | Type | Description | Default |
| ---- | ---- | ----------- | ------- |
| `name` | `str` | Dependency name to retrieve | `Required` |

**Returns:** `'Service'` - Dependency service

</Tabs.Tab>

<Tabs.Tab>

### initialize() 🔧

```python filename='method.py'
def initialize(self) -> 'None'
```

Initialize service and its dependencies synchronously.

</Tabs.Tab>

<Tabs.Tab>

### initialize_apps() 🔧

```python filename='method.py'
def initialize_apps(self)
```

Initialize apps.

</Tabs.Tab>

<Tabs.Tab>

### initialize_services() 🔧

```python filename='method.py'
def initialize_services(self)
```

Initialize app service dependencies synchronously.

</Tabs.Tab>

<Tabs.Tab>

### shutdown() 🔧

```python filename='method.py'
def shutdown(self) -> 'None'
```

Shutdown app and cleanup.

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ShutdownError` | If shutdown fails |

</Tabs.Tab>

<Tabs.Tab>

### shutdown_apps() 🔧

```python filename='method.py'
def shutdown_apps(self)
```

Shutdown app and cleanup.

**Raises:**

| Exception | Description |
| --------- | ----------- |
| `ShutdownError` | If shutdown fails |

</Tabs.Tab>

<Tabs.Tab>

### shutdown_services() 🔧

```python filename='method.py'
def shutdown_services(self)
```

Shutdown app service dependencies synchronously.

</Tabs.Tab>

<Tabs.Tab>

### start() 🔧

```python filename='method.py'
def start(self, context: 'ContextProtocol | None' = None) -> 'None'
```

Run the app.

</Tabs.Tab>

</Tabs>

### Source Code

```python filename='source.py'
class SyncApp(
    SyncAppInitializer[SyncStateT, SyncExecutorT],
    SyncCommonAppStateHandler[SyncStateT, SyncExecutorT],
    SyncAppExecutionHandler[SyncStateT, SyncExecutorT],
    SyncAppServicesHandler[SyncStateT, SyncExecutorT],
    SyncAppComposer[SyncStateT, SyncExecutorT],
):
    pass

```

## Source Code

```python filename='source.py'
"""
Apps for synchronous and asynchronous functionality.

Apps are constructed by composing various components, including initialization,
execution handling, service handling, and state management.
The `AsyncApp` class is designed for asynchronous applications, while the `SyncApp` class
is designed for synchronous applications.

The `AsyncApp` class inherits from:
- `AsyncAppInitializer`: Handles asynchronous app initialization.
- `AsyncCommonAppStateHandler`: Manages the app's state asynchronously.
- `AsyncAppExecutionHandler`: Handles asynchronous execution of app functions.
- `AsyncAppServicesHandler`: Manages asynchronous service handling.

The `SyncApp` class inherits from:
- `SyncAppInitializer`: Handles synchronous app initialization.
- `SyncCommonAppStateHandler`: Manages the app's state synchronously.
- `SyncAppExecutionHandler`: Handles synchronous execution of app functions.
- `SyncAppServicesHandler`: Manages synchronous service handling.
"""

from __future__ import annotations

from .composer import AsyncAppComposer, SyncAppComposer
from .handlers.execution_handler import AsyncAppExecutionHandler, SyncAppExecutionHandler
from .handlers.service_handler import AsyncAppServicesHandler, SyncAppServicesHandler
from .handlers.state_handler import AsyncCommonAppStateHandler, SyncCommonAppStateHandler
from .initializer import AsyncAppInitializer, SyncAppInitializer
from .types import ExecutorT, StateT, SyncExecutorT, SyncStateT

__all__ = [
    "AsyncApp",
    "SyncApp",
]


class AsyncApp(
    AsyncAppInitializer[StateT, ExecutorT],
    AsyncCommonAppStateHandler[StateT, ExecutorT],
    AsyncAppExecutionHandler[StateT, ExecutorT],
    AsyncAppServicesHandler[StateT, ExecutorT],
    AsyncAppComposer[StateT, ExecutorT],
):
    pass


class SyncApp(
    SyncAppInitializer[SyncStateT, SyncExecutorT],
    SyncCommonAppStateHandler[SyncStateT, SyncExecutorT],
    SyncAppExecutionHandler[SyncStateT, SyncExecutorT],
    SyncAppServicesHandler[SyncStateT, SyncExecutorT],
    SyncAppComposer[SyncStateT, SyncExecutorT],
):
    pass

```

