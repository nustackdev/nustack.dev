---
title: service
description: "Nu service domain: flat/capability DSL, sibling of shape."
---

Module `nu.domains.service`.

Nu service domain: flat/capability DSL, sibling of shape.

## Ref

| Name | Sort | Call | Meaning |
| --- | --- | --- | --- |
| [MethodRef](#methodref) | `ref` | `MethodRef(name, owner_service=None)` | A Ref addressing a Method on a Service. Leaf: address rides in _payload. |

### MethodRef

A Ref addressing a Method on a Service. Leaf: address rides in _payload.

```python
MethodRef(name, owner_service=None)
```

Path `nu.domains.service.MethodRef`. Kind `Ref`, sort `ref`, cardinality `scalar`.

Undocumented: example.
